// Re-export of the build-generated waiting-room renderer module from the
// runtime root. The build replace map rewrites WAITING_ROOM_RENDERER to the
// bundled user module, or to a null stub when no renderer is configured.
import waitingRoomRenderer from 'WAITING_ROOM_RENDERER';

export { waitingRoomRenderer };
