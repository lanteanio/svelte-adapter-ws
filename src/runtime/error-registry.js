/**
 * Stable search keys for operator-facing adapter failures. Keep messagePrefix
 * equal to the invariant beginning of the emitted text; dynamic host, path,
 * timeout, or native-loader details follow it at runtime.
 *
 * `emission` says which shape the runtime actually produces, because they differ
 * and a prefix that never appears in a log is worse than no prefix:
 *
 * - `thrown`   - an Error message with no diagnostic line around it.
 * - `direct`   - emitOperationalEvent(), which prints
 *                `[svelte-adapter-ws] <event>: <message>` plus an attributes
 *                JSON tail.
 * - `head`     - only the formatDiagnostic line head is invariant; severity
 *                and message vary by call site, so the prefix stops where the
 *                variation begins.
 * - `console`  - a plain console line printed through adapterConsoleLine()
 *                so the emitted text IS the registry's prefix plus the
 *                call-site detail and the stable ID tag.
 */
export const ADAPTER_ERROR_IDS = Object.freeze({
	LISTEN: 'ADAPTER-ERR-LISTEN',
	VITE_LOAD: 'ADAPTER-ERR-VITE-LOAD',
	VITE_RELOAD: 'ADAPTER-ERR-VITE-RELOAD',
	REQUEST_TIMEOUT: 'ADAPTER-ERR-REQUEST-TIMEOUT',
	REQUEST_CLOSED: 'ADAPTER-ERR-REQUEST-CLOSED',
	INVARIANT: 'ADAPTER-ERR-INVARIANT',
	WARMUP_RENDER: 'ADAPTER-ERR-WARMUP-RENDER',
	PRESSURE_RUNAWAY_PUBLISHER: 'ADAPTER-ERR-PRESSURE-RUNAWAY-PUBLISHER',
	PRESSURE_TOPIC_REGISTRY: 'ADAPTER-ERR-PRESSURE-TOPIC-REGISTRY',
	RESUME_HOOK: 'ADAPTER-ERR-RESUME-HOOK',
	AUTHENTICATE: 'ADAPTER-ERR-AUTHENTICATE',
	SSR: 'ADAPTER-ERR-SSR',
	UPGRADE_HOOK: 'ADAPTER-ERR-UPGRADE-HOOK',
	ATTRIBUTION_HOOK: 'ADAPTER-ERR-ATTRIBUTION',
	SUBSCRIBE_BATCH_HOOK: 'ADAPTER-ERR-SUBSCRIBE-BATCH-HOOK',
	SUBSCRIBE_BATCH_RESULT: 'ADAPTER-ERR-SUBSCRIBE-BATCH-RESULT',
	SUBSCRIBE_HOOK: 'ADAPTER-ERR-SUBSCRIBE-HOOK',
	TLS_RELOAD_SKIPPED: 'ADAPTER-ERR-TLS-RELOAD-SKIPPED',
	TLS_WATCH: 'ADAPTER-ERR-TLS-WATCH',
	CLUSTER_CONFIG_WORKERS: 'ADAPTER-ERR-CLUSTER-CONFIG-WORKERS',
	CLUSTER_CONFIG_COMPUTE: 'ADAPTER-ERR-CLUSTER-CONFIG-COMPUTE',
	CLUSTER_CONFIG_MODE: 'ADAPTER-ERR-CLUSTER-CONFIG-MODE',
	CLUSTER_CONFIG_REUSEPORT: 'ADAPTER-ERR-CLUSTER-CONFIG-REUSEPORT',
	CLUSTER_CONFIG_NODE: 'ADAPTER-ERR-CLUSTER-CONFIG-NODE',
	CLUSTER_CONFIG_ACCEPTOR: 'ADAPTER-ERR-CLUSTER-CONFIG-ACCEPTOR',
	CLUSTER_WORKER_ERROR: 'ADAPTER-ERR-CLUSTER-WORKER-ERROR',
	WORKER_RESTART_LIMIT: 'ADAPTER-ERR-WORKER-RESTART-LIMIT',
	WORKER_EXIT_FORCED: 'ADAPTER-ERR-WORKER-EXIT-FORCED',
	RELAY_SPILL_QUARANTINE: 'ADAPTER-ERR-RELAY-SPILL-QUARANTINE',
	RELAY_SPILL_OVERFLOW: 'ADAPTER-ERR-RELAY-SPILL-OVERFLOW',
	RELAY_FRAME_OVERSIZED: 'ADAPTER-ERR-RELAY-FRAME-OVERSIZED',
	RELAY_FRAME_REFUSED: 'ADAPTER-ERR-RELAY-FRAME-REFUSED',
	TLS_PRIMARY_BOOT_READ: 'ADAPTER-ERR-TLS-PRIMARY-BOOT-READ',
	TLS_PRIMARY_RELOAD_READ: 'ADAPTER-ERR-TLS-PRIMARY-RELOAD-READ',
	TLS_PRIMARY_WATCH: 'ADAPTER-ERR-TLS-PRIMARY-WATCH',
	TLS_DEGRADED_EXPIRY: 'ADAPTER-ERR-TLS-DEGRADED-EXPIRY',
	SHUTDOWN_LISTENER_THREW: 'ADAPTER-ERR-SHUTDOWN-LISTENER-THREW',
	SHUTDOWN_REQUESTS_DROPPED: 'ADAPTER-ERR-SHUTDOWN-REQUESTS-DROPPED',
	SHUTDOWN_LISTENERS_UNSETTLED: 'ADAPTER-ERR-SHUTDOWN-LISTENERS-UNSETTLED',
	SHUTDOWN_FAILED: 'ADAPTER-ERR-SHUTDOWN-FAILED',
	SENDTO_ASYNC_FILTER: 'ADAPTER-ERR-SENDTO-ASYNC-FILTER',
	WS_SHUTDOWN_HOOK_THREW: 'ADAPTER-ERR-WS-SHUTDOWN-HOOK-THREW',
	MESSAGE_HOOK: 'ADAPTER-ERR-MESSAGE-HOOK',
	RECOVER_HOOK: 'ADAPTER-ERR-RECOVER-HOOK',
	DIAGNOSTIC_RECORD_SHAPE: 'ADAPTER-ERR-DIAGNOSTIC-RECORD-SHAPE',
	DIAGNOSTIC_RENDER_COLLAPSE: 'ADAPTER-ERR-DIAGNOSTIC-RENDER-COLLAPSE',
	DIAGNOSTIC_CONSOLE_WRITE: 'ADAPTER-ERR-DIAGNOSTIC-CONSOLE-WRITE',
	DIAGNOSTIC_SINK_NOTICE: 'ADAPTER-ERR-DIAGNOSTIC-SINK-NOTICE',
	METRICS_MODULE_SHAPE: 'ADAPTER-ERR-METRICS-MODULE-SHAPE',
	METRICS_INSTRUMENT: 'ADAPTER-ERR-METRICS-INSTRUMENT',
	POSTURE_EXPORT_DISABLED: 'ADAPTER-ERR-POSTURE-EXPORT-DISABLED',
	UPGRADE_DEFERRED: 'ADAPTER-ERR-UPGRADE-DEFERRED',
	WAITING_ROOM_FALLBACK: 'ADAPTER-ERR-WAITING-ROOM-FALLBACK'
});

const HEAD = '[lantean/diagnostic source=svelte-adapter-ws component=';

/** Builds the invariant head of a directly-emitted diagnostic line. */
function direct(component, event, severity, message) {
	return HEAD + component + ' event=' + event + ' severity=' + severity + '] ' + message;
}

export const ADAPTER_ERROR_REGISTRY = Object.freeze([
	Object.freeze({
		id: ADAPTER_ERROR_IDS.LISTEN,
		code: 'LISTEN_FAILED',
		event: 'runtime.listen.failed',
		component: 'runtime.lifecycle',
		severity: 'error',
		emission: 'direct',
		problemPrefix: null,
		messagePrefix: direct('runtime.lifecycle', 'runtime.listen.failed', 'error', 'Failed to bind '),
		cause: 'The configured address or port could not be bound, or the process lacks permission.',
		consequence: 'The process never becomes ready and exits with status 1.',
		automaticRecovery: 'None inside the process. If a process manager restarts it, the replacement retries the same bind and a persistent conflict fails the same way each time.',
		nextAction: 'Check address availability, port conflicts, and bind permissions, then restart the process.',
		sources: Object.freeze(['src/runtime/index.js', 'src/runtime/handler/lifecycle.js']),
		anchor: 'adapter-err-listen',
		help: 'docs/errors.md#adapter-err-listen',
		link: 'https://svti.me/listen-failed'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.VITE_LOAD,
		code: null,
		event: 'vite.handler.load-failed',
		component: 'vite.websocket',
		severity: 'error',
		emission: 'composed',
		problemPrefix: 'Initial loading of the WebSocket handler',
		messagePrefix: '[lantean/diagnostic source=svelte-adapter-ws component=vite.websocket event=vite.handler.load-failed severity=error] vite.handler.load-failed: Initial loading of the WebSocket handler',
		cause: 'The initial development WebSocket handler or one of its imports failed to load.',
		consequence: 'The Vite HTTP server stays active, but WebSocket upgrades return HTTP 500 until a handler loads.',
		automaticRecovery: 'Vite retries the handler when its module graph changes again.',
		nextAction: 'Fix the reported module error and save the handler or one of its dependencies; a dev-server restart is not required.',
		sources: Object.freeze(['src/vite.js']),
		anchor: 'adapter-err-vite-load',
		help: 'docs/errors.md#adapter-err-vite-load',
		link: 'https://svti.me/ws-handler-load'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.VITE_RELOAD,
		code: null,
		event: 'vite.handler.reload-failed',
		component: 'vite.websocket',
		severity: 'error',
		emission: 'composed',
		problemPrefix: 'Hot reloading of the WebSocket handler',
		messagePrefix: '[lantean/diagnostic source=svelte-adapter-ws component=vite.websocket event=vite.handler.reload-failed severity=error] vite.handler.reload-failed: Hot reloading of the WebSocket handler',
		cause: 'A development handler hot reload failed after an earlier handler had loaded.',
		consequence: 'Existing WebSocket connections keep the previous handler, but new upgrades return HTTP 500 until recovery.',
		automaticRecovery: 'Vite retries the handler when its module graph changes again.',
		nextAction: 'Fix the reported module error and save the handler or one of its dependencies; a dev-server restart is not required.',
		sources: Object.freeze(['src/vite.js']),
		anchor: 'adapter-err-vite-reload',
		help: 'docs/errors.md#adapter-err-vite-reload',
		link: 'https://svti.me/ws-handler-load'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.REQUEST_TIMEOUT,
		code: null,
		event: 'websocket.request.timeout',
		component: null,
		severity: null,
		emission: 'thrown',
		problemPrefix: null,
		messagePrefix: 'request timed out',
		cause: 'A platform.request reply did not arrive within timeoutMs; the recipient may already have executed the request.',
		consequence: 'The caller promise rejects while the remote operation outcome remains unknown.',
		automaticRecovery: 'None. The adapter does not retry requests because replay may duplicate an operation.',
		nextAction: 'Reconcile application state first, or retry only through an idempotent operation; then investigate the handler, connection, and measured timeout budget.',
		sources: Object.freeze(['src/runtime/handler/platform.js']),
		anchor: 'adapter-err-request-timeout',
		help: 'docs/errors.md#adapter-err-request-timeout',
		link: 'https://svti.me/request-timeout'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.REQUEST_CLOSED,
		code: null,
		event: 'websocket.request.connection-closed',
		component: null,
		severity: null,
		emission: 'thrown',
		problemPrefix: null,
		messagePrefix: 'connection closed',
		cause: 'The target WebSocket closed around a platform.request - before the request frame could be sent at all, or with the frame already handed to the transport and unanswered.',
		consequence: 'The caller promise rejects. The rejection detail names which side of transmission the close landed on: a frame that was never sent leaves the remote outcome known - nothing was requested - while a frame handed to the transport leaves it unknown.',
		automaticRecovery: 'None. The adapter does not retry requests because replay may duplicate an operation.',
		nextAction: 'Read the rejection detail first. A request whose frame was never sent is safe to retry as-is once the connection recovers; a sent but unanswered request must be reconciled or retried only through an idempotent operation.',
		sources: Object.freeze(['src/runtime/handler/platform.js']),
		anchor: 'adapter-err-request-closed',
		help: 'docs/errors.md#adapter-err-request-closed',
		link: 'https://svti.me/request-closed'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.INVARIANT,
		code: null,
		event: 'invariant.violated',
		component: 'runtime.assertion',
		severity: null,
		emission: 'head',
		problemPrefix: null,
		messagePrefix: HEAD + 'runtime.assertion event=invariant.violated severity=',
		cause: 'A framework-internal assertion failed. The message is the assertion category and the severity is chosen by the call site, so both vary.',
		consequence: 'Depends on the tier. A recorded violation appears in the platform.assertions map; a development-only assertion is logged and thrown WITHOUT being recorded there, so an empty map does not mean none fired. At the fatal tier the process is scheduled to exit with a dedicated status code.',
		automaticRecovery: 'None for the condition itself. A fatal-tier violation exits the process; recovery is the process manager restarting it.',
		nextAction: 'Read the severity first, because it selects the tier and therefore the blast radius, then the category and context attributes. These are library-internal invariants, so a violation is an adapter defect rather than an application misconfiguration; report it with both attributes.',
		sources: Object.freeze(['src/runtime/utils/assertions.js']),
		anchor: 'adapter-err-invariant',
		help: 'docs/errors.md#adapter-err-invariant'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.WARMUP_RENDER,
		code: null,
		event: 'runtime.warmup.render-failed',
		component: 'runtime.warmup',
		severity: 'warn',
		emission: 'direct',
		problemPrefix: 'A boot warmup render failed; readiness proceeds without it.',
		messagePrefix: direct('runtime.warmup', 'runtime.warmup.render-failed', 'warn', 'A boot warmup render failed; readiness proceeds without it.'),
		cause: 'Rendering a configured warmup path through the SSR engine during boot threw. The warmup runs the app\'s own server hooks and load functions for that path, so the throw is almost always in application boot-path code (a load that assumes a real request header, a resource not ready at boot), not in the adapter.',
		consequence: 'That path is not pre-warmed, so the first real request to it after readiness pays the cold-render cost the warmup exists to remove. Nothing else is affected: readiness still commits and every other configured path still warms.',
		automaticRecovery: 'Yes. The first real request renders the path normally and warms it from then on; the warmup does not retry.',
		nextAction: 'Read the attached error and the path it names. If the render depends on request context a warmup cannot supply, guard that code behind platform.isWarmupRequest, or drop the path from the warmup set. A warmup render that fails every boot means the path is not safely renderable without a real client.',
		sources: Object.freeze(['src/runtime/handler/warmup.js']),
		anchor: 'adapter-err-warmup-render',
		help: 'docs/errors.md#adapter-err-warmup-render'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.PRESSURE_RUNAWAY_PUBLISHER,
		code: null,
		event: 'pressure.runaway-publisher',
		component: 'runtime.pressure',
		severity: 'warn',
		emission: 'direct',
		problemPrefix: 'A publisher crossed a configured per-topic pressure threshold.',
		messagePrefix: direct('runtime.pressure', 'pressure.runaway-publisher', 'warn', 'A publisher crossed a configured per-topic pressure threshold.'),
		cause: 'One topic exceeded its configured publish pressure threshold. The event is emitted only when no onPublishRate listener is registered, and it is latched per topic: one line when the topic crosses the threshold, re-armed only after the topic stays below it for a minute of consecutive samples.',
		consequence: 'Nothing is dropped by this event alone. It is the early signal that one topic is consuming a disproportionate share of outbound capacity.',
		automaticRecovery: 'None. Nothing throttles the publisher on the strength of this threshold.',
		nextAction: 'Identify the topic from the attributes and decide whether the rate is intended. The line is suppressed entirely while an onPublishRate listener is registered, so its absence is not evidence the condition ended - read platform.pressure for that. Left alone, a runaway publisher is what later produces slow-consumer disconnects on unrelated topics.',
		sources: Object.freeze(['src/runtime/handler/pressure.js']),
		anchor: 'adapter-err-pressure-runaway-publisher',
		help: 'docs/errors.md#adapter-err-pressure-runaway-publisher'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.PRESSURE_TOPIC_REGISTRY,
		code: null,
		event: 'pressure.topic-registry-high',
		component: null,
		severity: 'warn',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[svelte-adapter-ws] the per-topic seq registry reached ',
		cause: 'The number of distinct topics holding a live seq counter passed the warning threshold.',
		consequence: 'Nothing is refused at this threshold. Topic bookkeeping grows with cardinality, so this is the memory-growth signal.',
		automaticRecovery: 'None. Cardinality is not reduced in response to the threshold.',
		nextAction: 'Check whether topic names embed unbounded identifiers (per-user, per-request). The line fires once per process, so it will not tell you whether cardinality later fell or kept climbing - the naming scheme is what settles that. Unbounded cardinality is a slow leak rather than a spike, so act at the warning rather than at exhaustion.',
		sources: Object.freeze(['src/runtime/handler/platform.js']),
		anchor: 'adapter-err-pressure-topic-registry',
		help: 'docs/errors.md#adapter-err-pressure-topic-registry'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.RESUME_HOOK,
		code: null,
		event: 'resume.hook-failed',
		component: 'runtime.resume',
		severity: 'error',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[svelte-adapter-ws] the resume hook threw',
		cause: 'The application resume hook threw while answering a client GAP-FILL request, so some or all of the replay frames it owed were never sent. The same hook failing on the subscribe-time backfill is ADAPTER-ERR-RECOVER-HOOK instead.',
		consequence: 'The client is still sent `resumed`, because that ack is not conditional on the hook. It therefore believes its gap was handled and reports nothing. Whether the history is actually recovered depends on whether the subscribe frames it sends next carry recover offsets; if they do not, the gap is permanent and silent on both sides.',
		automaticRecovery: 'None for the gap. No fallback subscribe is triggered by this failure - the client simply continues its normal sequence.',
		nextAction: 'Fix the hook if resume coverage matters for these topics, and do not read a `resumed` ack as evidence a gap was filled. Clients that subscribe with recover offsets recover anyway; clients that do not are missing history without any signal.',
		sources: Object.freeze(['src/runtime/handler/realtime.js']),
		anchor: 'adapter-err-resume-hook',
		help: 'docs/errors.md#adapter-err-resume-hook'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.AUTHENTICATE,
		code: null,
		event: 'runtime.authenticate.failed',
		component: 'runtime.authenticate',
		severity: 'error',
		emission: 'direct',
		problemPrefix: 'The WebSocket authentication endpoint failed.',
		messagePrefix: direct('runtime.authenticate', 'runtime.authenticate.failed', 'error', 'The WebSocket authentication endpoint failed.'),
		cause: 'The application `authenticate` export threw or rejected while answering its HTTP POST endpoint, which the client posts to before opening its WebSocket.',
		consequence: 'That POST is answered 500. This is an ordinary HTTP route rather than the upgrade path, so no upgrade is refused and established connections are untouched; a client that treats the failed POST as fatal never goes on to open its WebSocket.',
		automaticRecovery: 'None for the failed request. The client may post again, which runs the hook again.',
		nextAction: 'Read the attached error and requestId and fix the hook. Look at the authentication endpoint and its dependencies, not at the upgrade path: the two are separate routes and this event never comes from an upgrade.',
		sources: Object.freeze(['src/runtime/handler.js']),
		anchor: 'adapter-err-authenticate',
		help: 'docs/errors.md#adapter-err-authenticate'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.SSR,
		code: null,
		event: 'runtime.ssr.failed',
		component: 'runtime.ssr',
		severity: 'error',
		emission: 'direct',
		problemPrefix: 'SvelteKit request handling failed.',
		messagePrefix: direct('runtime.ssr', 'runtime.ssr.failed', 'error', 'SvelteKit request handling failed.'),
		cause: 'The SvelteKit server handler threw while rendering or handling a request.',
		consequence: 'A failure before the response starts is answered with an error response. A response already streaming its body is aborted instead, so the client sees the truncation rather than a clean end that reads as a complete response. Other requests and WebSocket connections are unaffected.',
		automaticRecovery: 'None for the failed request.',
		nextAction: 'Read the attached error. This is application rendering code rather than adapter transport, so the fault is normally in a route, hook, or load function.',
		sources: Object.freeze(['src/runtime/handler/ssr.js']),
		anchor: 'adapter-err-ssr',
		help: 'docs/errors.md#adapter-err-ssr'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.UPGRADE_HOOK,
		code: null,
		event: 'runtime.websocket-upgrade.failed',
		component: 'runtime.websocket-upgrade',
		severity: 'error',
		emission: 'direct',
		problemPrefix: 'The WebSocket upgrade hook failed.',
		messagePrefix: direct('runtime.websocket-upgrade', 'runtime.websocket-upgrade.failed', 'error', 'The WebSocket upgrade hook failed.'),
		cause: 'The application upgrade hook threw while a client was being upgraded.',
		consequence: 'That upgrade does not complete and the client cannot open its WebSocket.',
		automaticRecovery: 'None. The client retries by reconnecting, which runs the hook again.',
		nextAction: 'Read the attached error and fix the hook. Persistent failure presents to users as a connection that never establishes, while HTTP continues to work.',
		sources: Object.freeze(['src/runtime/handler.js']),
		anchor: 'adapter-err-upgrade-hook',
		help: 'docs/errors.md#adapter-err-upgrade-hook'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.ATTRIBUTION_HOOK,
		code: null,
		event: 'runtime.websocket-attribution.failed',
		component: 'runtime.websocket-attribution',
		severity: 'error',
		emission: 'direct',
		problemPrefix: 'The WebSocket attribution hook failed; the connection was refused at open.',
		messagePrefix: direct('runtime.websocket-attribution', 'runtime.websocket-attribution.failed', 'error', 'The WebSocket attribution hook failed; the connection was refused at open.'),
		cause: 'The handler module\'s `attribution` export threw, returned a promise, returned a misshaped result, or returned an id outside the allowed form (a string of [a-zA-Z0-9_-], at most 64 characters).',
		consequence: 'That connection is closed with code 1008 before the application open hook runs. Attribution is fail-closed: a connection that cannot be attributed is refused rather than admitted unattributed, because an unattributed admission would silently stand down every tenant-scoped limit that reads the attribution.',
		automaticRecovery: 'None for that connection. The client may reconnect, which runs the resolver again against a fresh userData.',
		nextAction: 'Read the attached error and fix the `attribution` export: return { tenantId?, principalId?, entitlement? } synchronously - each present value a string of [a-zA-Z0-9_-] with 1-64 characters - or null/undefined for an unattributed connection. Resolve identity itself in the upgrade hook; attribution only derives from the userData that hook produced.',
		sources: Object.freeze(['src/runtime/handler.js']),
		anchor: 'adapter-err-attribution',
		help: 'docs/errors.md#adapter-err-attribution'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.SUBSCRIBE_BATCH_HOOK,
		code: null,
		event: 'subscribe.batch-hook-failed',
		component: 'runtime.subscribe',
		severity: 'error',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[svelte-adapter-ws] the subscribeBatch hook threw',
		cause: 'The application subscribeBatch authorization hook threw.',
		consequence: 'Every topic in that batch is denied with INTERNAL_ERROR. Authorization is fail-closed, so a throwing hook denies rather than admits.',
		automaticRecovery: 'None. The client may retry the subscribe, which runs the hook again.',
		nextAction: 'Fix the hook. Because one throw denies the whole batch, a fault touching a single topic presents as a client that can subscribe to nothing.',
		sources: Object.freeze(['src/runtime/handler/platform.js', 'src/runtime/handler/realtime.js']),
		anchor: 'adapter-err-subscribe-batch-hook',
		help: 'docs/errors.md#adapter-err-subscribe-batch-hook'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.SUBSCRIBE_BATCH_RESULT,
		code: null,
		event: 'subscribe.batch-result-read-failed',
		component: 'runtime.subscribe',
		severity: 'error',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[svelte-adapter-ws] reading the subscribeBatch result threw',
		cause: 'The subscribeBatch hook returned a value whose properties threw while being read, typically a getter or a proxy.',
		consequence: 'Every topic in that batch is denied with INTERNAL_ERROR, exactly as though the hook itself had thrown.',
		automaticRecovery: 'None. The client may retry the subscribe.',
		nextAction: 'Return a plain object keyed by topic from the hook, and keep property reads on it free of side effects. An array is not that shape: its entries are read back under index keys, so its denials name topics like 0 and 1 and every real topic in the batch is silently allowed.',
		sources: Object.freeze(['src/runtime/handler/platform.js']),
		anchor: 'adapter-err-subscribe-batch-result',
		help: 'docs/errors.md#adapter-err-subscribe-batch-result'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.SUBSCRIBE_HOOK,
		code: null,
		event: 'subscribe.hook-failed',
		component: 'runtime.subscribe',
		severity: 'error',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[svelte-adapter-ws] the subscribe hook threw',
		cause: 'The application subscribe authorization hook threw for a single topic.',
		consequence: 'That subscribe is denied with INTERNAL_ERROR. Authorization is fail-closed, and the reason is deliberately distinct: returning false denies with FORBIDDEN, so a throw is reported as a fault rather than as a refusal.',
		automaticRecovery: 'None. The client may retry the subscribe, which runs the hook again.',
		nextAction: 'Read the attached error and fix the hook. A client seeing INTERNAL_ERROR rather than FORBIDDEN or UNAUTHENTICATED is being told this is a defect, not a permissions decision, so treat it as one and do not go looking at authorization rules first.',
		sources: Object.freeze(['src/runtime/handler/platform.js']),
		anchor: 'adapter-err-subscribe-hook',
		help: 'docs/errors.md#adapter-err-subscribe-hook'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.TLS_RELOAD_SKIPPED,
		code: null,
		event: 'tls.reload-skipped',
		component: 'runtime.tls',
		severity: 'warn',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[svelte-adapter-ws] [tls] certificate reload failed; serving the previous certificate',
		cause: 'A certificate change was seen on disk but not applied, usually because the new material was unreadable or incomplete at the moment it was read.',
		consequence: 'The server keeps serving the previous certificate. The renewal on disk is not in use, so the served certificate can expire while a valid one sits unread. READINESS PROBES STAY GREEN throughout, which is what makes this quiet.',
		automaticRecovery: 'The next watcher event retries the reload; a clean read applies the renewal.',
		nextAction: 'Confirm the served certificate matches the one on disk rather than assuming renewal succeeded - the probe cannot see this. Treat the warning as expiry risk, not noise.',
		sources: Object.freeze(['src/runtime/handler/tls.js']),
		anchor: 'adapter-err-tls-reload-skipped',
		help: 'docs/errors.md#adapter-err-tls-reload-skipped'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.TLS_WATCH,
		code: null,
		event: 'tls.watch-failed',
		component: 'runtime.tls',
		severity: 'error',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[svelte-adapter-ws] [tls] certificate watch ',
		cause: 'The filesystem watch on a certificate directory could not be established, or errored after arming (directory removed, permissions).',
		consequence: 'Certificate hot reload is off until restart; the served certificate stays on its current bytes. A renewal landing later is never picked up, and the failure surfaces much later as an expired certificate.',
		automaticRecovery: 'None: the watch is not retried, so this does not resolve without a restart.',
		nextAction: 'Fix the path or permissions and restart the process. Until then, treat certificate renewal as requiring a restart, and alert on certificate expiry independently.',
		sources: Object.freeze(['src/runtime/handler/tls.js']),
		anchor: 'adapter-err-tls-watch',
		help: 'docs/errors.md#adapter-err-tls-watch'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.CLUSTER_CONFIG_WORKERS,
		code: null,
		event: 'cluster.config.invalid-workers',
		component: null,
		severity: 'fatal',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: "[svelte-adapter-ws] Invalid CLUSTER_WORKERS value: '",
		cause: "CLUSTER_WORKERS is set to something other than a positive integer or 'auto'.",
		consequence: 'The cluster primary exits with status 1 before spawning any worker; the service never comes up.',
		automaticRecovery: 'None. Startup configuration is validated once, at boot.',
		nextAction: "Set CLUSTER_WORKERS to a positive integer or 'auto' (or unset it) and restart.",
		sources: Object.freeze(['src/runtime/index.js']),
		anchor: 'adapter-err-cluster-config-workers',
		help: 'docs/errors.md#adapter-err-cluster-config-workers'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.CLUSTER_CONFIG_COMPUTE,
		code: null,
		event: 'cluster.config.invalid-compute-count',
		component: null,
		severity: 'fatal',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[svelte-adapter-ws] websocket.workers.compute (',
		cause: 'websocket.workers.compute is greater than or equal to the total worker count, which would leave no I/O worker to listen.',
		consequence: 'The cluster primary exits with status 1 before spawning any worker; the service never comes up.',
		automaticRecovery: 'None. Startup configuration is validated once, at boot.',
		nextAction: 'Lower websocket.workers.compute or raise CLUSTER_WORKERS so at least one I/O worker remains.',
		sources: Object.freeze(['src/runtime/index.js']),
		anchor: 'adapter-err-cluster-config-compute',
		help: 'docs/errors.md#adapter-err-cluster-config-compute'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.CLUSTER_CONFIG_MODE,
		code: null,
		event: 'cluster.config.invalid-mode',
		component: null,
		severity: 'fatal',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: "[svelte-adapter-ws] Invalid CLUSTER_MODE: '",
		cause: 'CLUSTER_MODE is set to an unknown value.',
		consequence: 'The cluster primary exits with status 1 before spawning any worker; the service never comes up.',
		automaticRecovery: 'None. Startup configuration is validated once, at boot.',
		nextAction: "Use 'reuseport' (Linux), or unset CLUSTER_MODE - reuseport is also the default and the only mode this runtime has.",
		sources: Object.freeze(['src/runtime/index.js']),
		anchor: 'adapter-err-cluster-config-mode',
		help: 'docs/errors.md#adapter-err-cluster-config-mode'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.CLUSTER_CONFIG_REUSEPORT,
		code: null,
		event: 'cluster.config.reuseport-unsupported',
		component: null,
		severity: 'fatal',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[svelte-adapter-ws] CLUSTER_WORKERS requires Linux (SO_REUSEPORT accept distribution is not available on ',
		cause: 'CLUSTER_WORKERS is set on a platform other than Linux. Every io worker binds the shared port itself with SO_REUSEPORT and the kernel distributes accepted connections across the listeners; off Linux the bind fails outright (Windows, macOS) or the kernel routes every accept to one listener.',
		consequence: 'The cluster primary exits with status 1 before spawning any worker; the service never comes up. Refusing beats booting a fleet whose extra workers can never take traffic.',
		automaticRecovery: 'None. Startup configuration is validated once, at boot.',
		nextAction: 'Deploy on Linux to keep the in-process cluster, or unset CLUSTER_WORKERS and run one process per core under your process manager behind a load balancer.',
		sources: Object.freeze(['src/runtime/index.js']),
		anchor: 'adapter-err-cluster-config-reuseport',
		help: 'docs/errors.md#adapter-err-cluster-config-reuseport'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.CLUSTER_CONFIG_NODE,
		code: null,
		event: 'cluster.config.node-unsupported',
		component: null,
		severity: 'fatal',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[svelte-adapter-ws] CLUSTER_WORKERS requires Node 22.12 or newer (this process runs Node ',
		cause: 'CLUSTER_WORKERS is set on a Node version whose listen() silently ignores the reusePort option (it exists from 22.12 and 23.1).',
		consequence: 'The cluster primary exits with status 1 before spawning any worker. Without this refusal the first io worker would bind normally and every sibling would crash-loop on EADDRINUSE until the restart budget took the whole process down - minutes of partial service ending in total outage.',
		automaticRecovery: 'None. Startup configuration is validated once, at boot.',
		nextAction: 'Upgrade Node to 22.12 or newer, or unset CLUSTER_WORKERS and run one process per core under your process manager.',
		sources: Object.freeze(['src/runtime/index.js']),
		anchor: 'adapter-err-cluster-config-node',
		help: 'docs/errors.md#adapter-err-cluster-config-node'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.CLUSTER_CONFIG_ACCEPTOR,
		code: null,
		event: 'cluster.config.acceptor-unsupported',
		component: null,
		severity: 'fatal',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[svelte-adapter-ws] CLUSTER_MODE=acceptor is not available on this runtime: moving an accepted socket between threads needs a native transport. ',
		cause: 'CLUSTER_MODE=acceptor asks one thread to accept connections and hand the live sockets to the other workers. Node core cannot transfer socket ownership across worker threads, so this runtime has no acceptor to offer.',
		consequence: 'The cluster primary exits with status 1 before spawning any worker; the service never comes up.',
		automaticRecovery: 'None. Startup configuration is validated once, at boot.',
		nextAction: 'Unset CLUSTER_MODE to use reuseport (the default, Linux), or deploy the native-transport family adapter where acceptor mode exists.',
		sources: Object.freeze(['src/runtime/index.js']),
		anchor: 'adapter-err-cluster-config-acceptor',
		help: 'docs/errors.md#adapter-err-cluster-config-acceptor'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.CLUSTER_WORKER_ERROR,
		code: null,
		event: 'cluster.worker-error',
		component: 'runtime.cluster',
		severity: 'error',
		emission: 'direct',
		problemPrefix: 'A worker thread reported an error.',
		messagePrefix: direct('runtime.cluster', 'cluster.worker-error', 'error', 'A worker thread reported an error.'),
		cause: 'A worker thread emitted an error event to the primary, which usually means it threw outside a request or failed during startup.',
		consequence: 'That worker is unhealthy. Its connections are lost when it exits, and cluster capacity drops until it is replaced.',
		automaticRecovery: "The supervisor replaces an exiting worker; the error itself is reported, not retried. Under a startup or import fault every replacement hits the same error, each attempt is charged against the slot's restart budget, and an exhausted slot takes the whole service down - the outcome ADAPTER-ERR-WORKER-RESTART-LIMIT documents from the other end.",
		nextAction: 'Read the attached error attribute. A repeating worker error at startup usually means a configuration or import fault that every replacement will hit as well.',
		sources: Object.freeze(['src/runtime/index.js']),
		anchor: 'adapter-err-cluster-worker-error',
		help: 'docs/errors.md#adapter-err-cluster-worker-error'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.WORKER_RESTART_LIMIT,
		code: null,
		event: 'cluster.worker.restart-limit',
		component: null,
		severity: 'fatal',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[svelte-adapter-ws] Worker restart limit reached for ',
		cause: 'A worker slot crashed and was respawned repeatedly without ever reaching stable uptime, exhausting its restart budget.',
		consequence: 'The primary exits with status 1 (its worker threads die with the process) and the whole service goes down until an orchestrator respawns it.',
		automaticRecovery: 'None inside the process. An orchestrator respawn, where one is configured, is the recovery path.',
		nextAction: 'Read the failing worker crash output above this line: the restart limit is the symptom and the repeated worker crash is the fault. A loop this fast is usually a boot-time error, not load.',
		sources: Object.freeze(['src/runtime/index.js']),
		anchor: 'adapter-err-worker-restart-limit',
		help: 'docs/errors.md#adapter-err-worker-restart-limit'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.WORKER_EXIT_FORCED,
		code: null,
		event: 'cluster.worker-exit-forced',
		component: null,
		severity: 'error',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[primary] worker ',
		cause: 'A worker was asked to exit and had not done so within the exit grace period, so the primary terminated that worker thread in place. A wedged event loop cannot process the exit request; terminating just the thread is safe here because a plain Node worker holds no native socket handles.',
		consequence: "Only the wedged worker dies: its connections drop and those clients reconnect onto a sibling. The slot respawns under the restart budget, so a worker that wedges on every boot eventually exhausts it (see ADAPTER-ERR-WORKER-RESTART-LIMIT).",
		automaticRecovery: 'Yes - the exit fires the normal restart path and a replacement occupies the same slot.',
		nextAction: 'Find why the worker would not exit. A blocked event loop is the usual cause - a synchronous hook, an unbounded loop, or a native call that does not return - and it will happen again at the next exit request. Most exit requests print their reason above this line; the one that does not is the shutdown budget expiring, where the request went to every worker at once.',
		sources: Object.freeze(['src/runtime/index.js']),
		anchor: 'adapter-err-worker-exit-forced',
		help: 'docs/errors.md#adapter-err-worker-exit-forced'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.RELAY_SPILL_QUARANTINE,
		code: null,
		event: 'cluster-relay.spill-quarantine',
		component: null,
		severity: 'error',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[primary] relay spill quarantining ',
		cause: "The primary could not hand relay traffic DOWN to this worker inside the worker's spill ceiling - its ring backlog crossed the byte limit, or the worker stopped making drain progress for longer than the age limit - so the primary quarantined it. The opposite direction, a worker that could not reach the primary, is ADAPTER-ERR-RELAY-SPILL-OVERFLOW.",
		consequence: "The primary stops forwarding relay traffic to that worker and asks it to exit, so its clients are dropped and reconnect onto a sibling. Until they do, that worker's subscribers were already missing whatever the ring could not deliver. Quarantine happens once per worker - the primary does not re-evaluate it - and the line names the reason, the bytes dropped and how long the backlog had been pending.",
		automaticRecovery: "The exit is a request, not a guarantee: quarantine posts a terminate message the quarantined worker's own event loop must process, and an AGE quarantine means exactly that loop stopped making progress. A worker that processes the request exits and the primary replaces it; one still wedged when the exit grace expires is terminated in place and its slot respawned - the mechanism ADAPTER-ERR-WORKER-EXIT-FORCED documents. Either way the dropped frames are not resent, so a client that was subscribed on that worker has a hole its own resume path must fill when it reconnects.",
		nextAction: "Read the reason on the line. An AGE spill means that worker stopped draining its ring - a blocked event loop is the usual cause, and it is the worker's own thread to profile, not the primary's. A BYTES spill can mean either: a peer merely behind on a ceiling sized too close to the largest relayed frame, where raising CLUSTER_RELAY_MAX_PENDING_KB to a few times that frame is the fix, or sustained fan-out the relay is undersized for, where a wider ceiling only delays the next spill. The droppedBytes on the line tells you which.",
		sources: Object.freeze(['src/runtime/relay-spill-policy.js']),
		anchor: 'adapter-err-relay-spill-quarantine',
		help: 'docs/errors.md#adapter-err-relay-spill-quarantine'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.RELAY_SPILL_OVERFLOW,
		code: null,
		event: 'cluster-relay.up-spill-overflow',
		component: 'runtime.cluster-relay',
		severity: 'error',
		emission: 'direct',
		problemPrefix: 'This worker could not hand its relay backlog to the primary within its spill ceiling and is exiting to be replaced.',
		messagePrefix: direct('runtime.cluster-relay', 'cluster-relay.up-spill-overflow', 'error', 'This worker could not hand its relay backlog to the primary within its spill ceiling and is exiting to be replaced.'),
		cause: 'The worker queued more relay bytes, or held them longer, than its spill ceiling allows while waiting on the primary.',
		consequence: 'The worker exits deliberately rather than growing an unbounded queue. Connections on it drop and those clients reconnect to whichever workers are still up.',
		automaticRecovery: 'Within the slot restart budget. The worker exits and the supervisor respawns it, but a slot that keeps exiting without reaching stable uptime exhausts that budget and the primary then exits the whole process (see ADAPTER-ERR-WORKER-RESTART-LIMIT). A blocked primary - the usual cause here - starves every worker at once, so repeated occurrences are the shape that reaches exhaustion rather than a series each worker recovers from.',
		nextAction: 'Read the reason, droppedBytes, and pendingAgeMs attributes. A blocked or slow primary is the usual cause; if the backlog is legitimate peak traffic, raise the relay ring pending ceilings. Check whether siblings are reporting this too - a process-wide cause will not resolve by replacing one worker.',
		sources: Object.freeze(['src/runtime/index.js']),
		anchor: 'adapter-err-relay-spill-overflow',
		help: 'docs/errors.md#adapter-err-relay-spill-overflow'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.RELAY_FRAME_OVERSIZED,
		code: null,
		event: 'cluster-relay.frame-oversized',
		component: 'runtime.cluster-relay',
		severity: 'error',
		emission: 'direct',
		problemPrefix: 'A worker sent a relay frame larger than this process will reassemble; its relay stream was stopped.',
		messagePrefix: direct('runtime.cluster-relay', 'cluster-relay.frame-oversized', 'error', 'A worker sent a relay frame larger than this process will reassemble; its relay stream was stopped.'),
		cause: 'A worker declared a relay frame above the reassembly ceiling, which is four times the configured relay frame ceiling. Either the ceiling is set far below real payloads, or the stream is corrupt.',
		consequence: 'That worker relay stream is stopped, so its cross-worker publishes no longer reach this process. Local delivery on the sending worker continues, which is what makes the split silent.',
		automaticRecovery: 'None for the stopped stream itself. The sending worker is expected to retire through its own spill overflow and be replaced, which is the path that actually restores its relay.',
		nextAction: 'Compare the declaredBytes and maxFrameBytes attributes. If the payload is legitimate, raise the relay frame ceiling; otherwise treat the stream as corrupt and replace the worker.',
		sources: Object.freeze(['src/runtime/index.js']),
		anchor: 'adapter-err-relay-frame-oversized',
		help: 'docs/errors.md#adapter-err-relay-frame-oversized'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.RELAY_FRAME_REFUSED,
		code: null,
		event: 'cluster-relay.frame-refused',
		component: 'runtime.cluster-relay',
		severity: 'warn',
		emission: 'direct',
		problemPrefix: 'A publish was too large for the cluster relay and was not sent to other workers. Local subscribers received it.',
		messagePrefix: direct('runtime.cluster-relay', 'cluster-relay.frame-refused', 'warn', 'A publish was too large for the cluster relay and was not sent to other workers. Local subscribers received it.'),
		cause: 'A publish exceeded the configured relay frame ceiling for cross-worker delivery.',
		consequence: 'Subscribers on this worker received the message and subscribers on every other worker did not. Clients therefore disagree about state depending on which worker they landed on.',
		automaticRecovery: 'None. The refused publish is not retried or fragmented.',
		nextAction: 'Reduce the payload size, or raise the relay frame ceiling to cover it. Treat repeated occurrences as a correctness problem rather than a capacity warning, because the split is invisible to clients.',
		sources: Object.freeze(['src/runtime/index.js']),
		anchor: 'adapter-err-relay-frame-refused',
		help: 'docs/errors.md#adapter-err-relay-frame-refused'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.TLS_PRIMARY_BOOT_READ,
		code: null,
		event: 'tls.primary.boot-read-failed',
		component: null,
		severity: 'error',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[tls] boot certificate unreadable on the primary (hot-reload broadcast stays armed)',
		cause: 'The cluster primary could not read or parse the boot certificate while arming the hot-reload watch.',
		consequence: 'Primary-side expiry observability starts blind: no baseline identity or expiry is recorded, so a later reload failure is reported without the number that says how urgent it is. Workers gate on their own certificate reads and keep serving; the reload broadcast stays armed.',
		automaticRecovery: 'The next reload that reads cleanly records identity and expiry.',
		nextAction: 'Verify the certificate path and PEM contents on the primary host.',
		sources: Object.freeze(['src/runtime/index.js']),
		anchor: 'adapter-err-tls-primary-boot-read',
		help: 'docs/errors.md#adapter-err-tls-primary-boot-read'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.TLS_PRIMARY_RELOAD_READ,
		code: null,
		event: 'tls.primary.reload-read-failed',
		component: null,
		severity: 'error',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[tls] renewed certificate unreadable on the primary (workers gate on their own reads)',
		cause: 'A certificate change was seen on disk but the renewed material was unreadable or incomplete when the primary read it.',
		consequence: 'The reload broadcast still goes out and every worker gates on its OWN read, so a primary-local failure (a read racing the renewal writer at the primary debounce instant) can leave the workers correctly swapped while only the primary is blind. What certainly failed is the primary side: no renewed identity or expiry is recorded, the primary enters the degraded TLS state with its expiry sentinel armed, and READINESS PROBES STAY GREEN. When the renewal itself is broken, every worker read fails the same way and the fleet keeps the previous certificate.',
		automaticRecovery: 'Every certificate change broadcasts again; the next change the primary reads cleanly records identity and expiry and clears the degraded state.',
		nextAction: 'Check whether the workers actually swapped (compare the served certificate against the renewal on disk) before assuming the fleet is stale, then fix the certificate material or the primary-host read.',
		sources: Object.freeze(['src/runtime/index.js']),
		anchor: 'adapter-err-tls-primary-reload-read',
		help: 'docs/errors.md#adapter-err-tls-primary-reload-read'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.TLS_PRIMARY_WATCH,
		code: null,
		event: 'tls.primary.watch-failed',
		component: null,
		severity: 'error',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[tls] primary cert watch failed to start, cluster hot-reload disabled (server keeps running)',
		cause: 'The filesystem watch on the certificate directory could not start on the cluster primary, commonly a not-yet-mounted secret volume or a mistyped path.',
		consequence: 'Cluster-wide certificate hot reload is off for the process lifetime: with no watcher on the primary, no worker is ever told to reload, so the whole fleet serves its current certificate until it expires. The primary enters the degraded TLS state; readiness probes stay green throughout.',
		automaticRecovery: 'None. The watch is not retried, so this does not resolve without a restart.',
		nextAction: 'Fix the path or permissions and restart the primary. Until then, treat certificate renewal as requiring a restart and alert on certificate expiry independently.',
		sources: Object.freeze(['src/runtime/index.js']),
		anchor: 'adapter-err-tls-primary-watch',
		help: 'docs/errors.md#adapter-err-tls-primary-watch'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.TLS_DEGRADED_EXPIRY,
		code: null,
		event: 'tls.degraded-expiry-alert',
		component: null,
		severity: 'error',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[svelte-adapter-ws] [tls] certificate hot-reload is DEGRADED (',
		cause: 'TLS hot-reload is in the degraded state on the cluster primary and the recorded certificate expiry is inside the alert window.',
		consequence: 'The figure is the last certificate the primary read cleanly, not necessarily what the workers serve - after a primary-local reload-read failure this alarm can count down against a certificate the fleet already replaced. When the renewal itself is broken the countdown is real: handshakes fail at the printed expiry while readiness probes stay green.',
		automaticRecovery: 'The alert re-checks hourly while degraded. A reload that succeeds clears the degraded state and silences it.',
		nextAction: 'Verify the served certificate against the renewal on disk first (see ADAPTER-ERR-TLS-PRIMARY-RELOAD-READ). If the fleet is genuinely stale, fix the certificate files now and restart the instance if the reload cannot be repaired before the printed expiry.',
		sources: Object.freeze(['src/runtime/index.js']),
		anchor: 'adapter-err-tls-degraded-expiry',
		help: 'docs/errors.md#adapter-err-tls-degraded-expiry'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.SHUTDOWN_LISTENER_THREW,
		code: null,
		event: 'shutdown.listener-threw',
		component: null,
		severity: 'error',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[svelte-adapter-ws] a sveltekit:shutdown listener failed',
		cause: 'A sveltekit:shutdown listener threw or rejected during shutdown; each listener is awaited, so both land here.',
		consequence: "That listener's cleanup did not complete. The throw is contained: remaining listeners still run, shutdown proceeds, and the exit is not held.",
		automaticRecovery: 'Not applicable; shutdown proceeds without the failed cleanup.',
		nextAction: 'Fix the listener, and check whatever it was tearing down (pools, final writes) for leaked state, because that teardown did not happen.',
		sources: Object.freeze(['src/runtime/index.js']),
		anchor: 'adapter-err-shutdown-listener-threw',
		help: 'docs/errors.md#adapter-err-shutdown-listener-threw'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.SHUTDOWN_REQUESTS_DROPPED,
		code: null,
		event: 'shutdown.requests-dropped',
		component: null,
		severity: 'error',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[svelte-adapter-ws] in-flight requests did not finish within the shutdown budget; closing ',
		cause: 'In-flight HTTP requests were still open when the configured shutdown budget expired.',
		consequence: 'The remaining open requests are dropped as the sockets close; their clients see resets. The drop is bounded and deliberate: the budget exists so a wedged request cannot hold the process open.',
		automaticRecovery: 'Not applicable; shutdown proceeds by design.',
		nextAction: 'Raise SHUTDOWN_TIMEOUT if legitimate requests need longer to drain, or find the handler that never finished. SHUTDOWN_TIMEOUT=0 removes the budget entirely and waits forever.',
		sources: Object.freeze(['src/runtime/handler/lifecycle.js']),
		anchor: 'adapter-err-shutdown-requests-dropped',
		help: 'docs/errors.md#adapter-err-shutdown-requests-dropped'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.SHUTDOWN_LISTENERS_UNSETTLED,
		code: null,
		event: 'shutdown.listeners-unsettled',
		component: null,
		severity: 'error',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[svelte-adapter-ws] shutdown cleanup exceeded the budget; draining now',
		cause: 'sveltekit:shutdown listeners or the ws shutdown hook were still pending when the cleanup budget expired.',
		consequence: 'The drain proceeds with that cleanup unfinished: final writes and teardowns still pending did not complete before the exit.',
		automaticRecovery: 'Not applicable; the budget exists so a wedged listener cannot hold the exit.',
		nextAction: 'Make the listener finish within the budget or raise SHUTDOWN_TIMEOUT; SHUTDOWN_TIMEOUT=0 removes the budget entirely and waits forever.',
		sources: Object.freeze(['src/runtime/index.js']),
		anchor: 'adapter-err-shutdown-listeners-unsettled',
		help: 'docs/errors.md#adapter-err-shutdown-listeners-unsettled'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.SHUTDOWN_FAILED,
		code: null,
		event: 'shutdown.failed',
		component: null,
		severity: 'error',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[svelte-adapter-ws] graceful shutdown failed',
		cause: 'The graceful shutdown sequence itself threw. Every application-supplied input is contained behind its own entry (a throwing ws shutdown hook, a rejecting or wedged sveltekit:shutdown listener, an overrunning drain), so this line means the sequence\'s own machinery raised - an adapter defect, or an application-side patch of a global the sequence reads, such as an instrumentation layer\'s rewrap of process or EventEmitter internals throwing when the listener list is read.',
		consequence: 'The orderly steps after the throw were skipped, so the shutdown was not clean; the process still exits rather than hanging.',
		automaticRecovery: 'Not applicable.',
		nextAction: 'Read the attached error. A stack through application instrumentation points at a broken global patch; anything else is adapter-owned and worth reporting with the attached error.',
		sources: Object.freeze(['src/runtime/index.js']),
		anchor: 'adapter-err-shutdown-failed',
		help: 'docs/errors.md#adapter-err-shutdown-failed'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.SENDTO_ASYNC_FILTER,
		code: null,
		event: 'ws.sendto.async-filter-refused',
		component: null,
		severity: 'error',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[ws] platform.sendTo filter returned a Promise; treating as fail-closed.',
		cause: 'A platform.sendTo filter returned a Promise. The filter must be synchronous, because sendTo iterates every active connection in one pass.',
		consequence: 'Every connection whose filter returns a Promise is skipped - fail-closed - on this and every later sendTo call, so the targeted delivery silently reaches nobody the filter cannot answer synchronously. The warning prints once per process.',
		automaticRecovery: 'None. The filter stays fail-closed until the code is fixed.',
		nextAction: 'Resolve the fields the filter needs into userData in your upgrade hook so the filter can read them synchronously.',
		sources: Object.freeze(['src/runtime/handler/platform.js']),
		anchor: 'adapter-err-sendto-async-filter',
		help: 'docs/errors.md#adapter-err-sendto-async-filter',
		link: 'https://svti.me/sendto-async'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.WS_SHUTDOWN_HOOK_THREW,
		code: null,
		event: 'ws.shutdown-hook.threw',
		component: null,
		severity: 'error',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[ws] the WebSocket shutdown hook threw',
		cause: 'The `shutdown` export of the WebSocket handler threw synchronously or rejected while the server was closing.',
		consequence: 'Whatever that hook was flushing did not finish - final writes, external deregistration, or draining a queue. The throw is contained and the teardown carries on regardless, so the loss is silent unless this line is read.',
		automaticRecovery: 'None. Shutdown is best-effort and proceeds without the hook.',
		nextAction: 'Fix the hook, then check whatever it was flushing for state left behind. The hook is awaited with { platform } during the drain and shares the shutdown cleanup budget, so long-running flushes must finish inside it.',
		sources: Object.freeze(['src/runtime/handler/realtime.js']),
		anchor: 'adapter-err-ws-shutdown-hook-threw',
		help: 'docs/errors.md#adapter-err-ws-shutdown-hook-threw'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.MESSAGE_HOOK,
		code: null,
		event: 'ws.message-hook.threw',
		component: null,
		severity: 'error',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[ws] the message hook threw',
		cause: 'The application or plugin `message` hook threw or rejected while handling a client frame.',
		consequence: "That client's connection is closed with code 1011 and the reason `Message handler error`; the cause stays server-side and is not sent to the client. Other connections are unaffected. A client that reconnects and replays the same frame is closed again.",
		automaticRecovery: 'None for the frame. The client sees a close, and reconnection is the client library\'s own resume path.',
		nextAction: 'Fix the hook, or catch inside it and answer the client deliberately. The error printed with this line is the original throw.',
		sources: Object.freeze(['src/runtime/utils/hook-boundary.js']),
		anchor: 'adapter-err-message-hook',
		help: 'docs/errors.md#adapter-err-message-hook'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.RECOVER_HOOK,
		code: null,
		event: 'ws.recover-hook.threw',
		component: null,
		severity: 'error',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[ws] the recover-on-subscribe hook threw',
		cause: 'The `resume` hook threw on the RECOVER-ON-SUBSCRIBE path - a client subscribing to a topic the server was asked to backfill - so the backlog that subscription should have replayed could not be produced. The same hook failing on a client resume frame is ADAPTER-ERR-RESUME-HOOK instead.',
		consequence: "The subscription itself still completes: the client is subscribed and receives live frames from that moment on, but the events it missed before subscribing are not delivered and no gap is reported to it. It looks like a working subscription with a hole at the start.",
		automaticRecovery: 'None. Live delivery continues; the missed range is not retried.',
		nextAction: 'Fix the hook or make it fail closed for the topics it cannot serve. A hook that throws for a topic it does not own should return an empty result for it instead.',
		sources: Object.freeze(['src/runtime/handler/realtime.js']),
		anchor: 'adapter-err-recover-hook',
		help: 'docs/errors.md#adapter-err-recover-hook'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.DIAGNOSTIC_RECORD_SHAPE,
		code: null,
		event: 'ws.diagnostic.record-shape',
		component: null,
		severity: 'error',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[ws] operational event dropped, invalid record shape',
		cause: 'Something emitted an operational diagnostic the runtime could not build a record from - a malformed event name, an unknown severity or data class, or a bad timestamp. A field that cannot be SERIALIZED is a different, quieter failure: the renderer absorbs it by printing the envelope with the attributes stripped, so the event still appears minus its attributes and no error line prints - the render path reports only its own total collapse (see ADAPTER-ERR-DIAGNOSTIC-RENDER-COLLAPSE).',
		consequence: 'That diagnostic is DROPPED: it reaches neither the configured sink nor the log, so the failure it was reporting leaves no structured trace. Everything else keeps emitting normally.',
		automaticRecovery: 'None for the dropped record. Telemetry deliberately never throws, so the emitting path continued as if it had been reported.',
		nextAction: 'Read the event name printed with this line and fix the emitter. If it is application or plugin code calling the diagnostic surface, check the record against the documented shape.',
		sources: Object.freeze(['src/runtime/diagnostic.js']),
		anchor: 'adapter-err-diagnostic-record-shape',
		help: 'docs/errors.md#adapter-err-diagnostic-record-shape'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.DIAGNOSTIC_RENDER_COLLAPSE,
		code: null,
		event: 'ws.diagnostic.render-collapse',
		component: null,
		severity: 'error',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[ws] diagnostic render failed: ',
		cause: "Formatting an operational diagnostic threw, and formatting it again with the attributes stripped threw as well. That SECOND failure is what makes this environmental rather than a bad record: the strip leaves only fields createDiagnostic produced and validated itself, every one a bounded string or number - the message is cut to 512 characters at creation - so no record this runtime accepted can fail the retry. What can fail it is the JSON serialization the format is built on: a replaced or wrapped `JSON.stringify`, a `toJSON` added to `Object.prototype`, an instrumentation agent that patched either. A value in the ATTRIBUTES that cannot be serialized is absorbed by the retry and never reaches this line, and a console that refuses the finished text prints the diagnostic-console-write line instead.",
		consequence: 'That diagnostic is lost. How much goes with it depends on what the serialization refuses: one that fails outright takes the whole operational stream, and one that refuses only certain shapes - a wrapper scrubbing a field, a size ceiling - takes every diagnostic matching that shape and lets the rest through. So a log that still carries other events does NOT mean this was isolated to the record named here; it means the refusal is selective. The request path is unaffected either way: the failure is contained so telemetry cannot take a worker down with it.',
		automaticRecovery: 'None for the lost record, and a later diagnostic printing normally is not recovery - under a selective refusal the ones that do not match keep working throughout, which is exactly what makes the gap easy to miss. Nothing is retried and nothing is unregistered; each event is formatted independently and meets the same serialization.',
		nextAction: "Do not start at the emitter or the record - neither can produce this line. Look for what changed this process's JSON serialization: an APM or instrumentation agent, a polyfill, a test harness stubbing `JSON.stringify`, or a dependency that added `toJSON` to `Object.prototype`. Do NOT clear it on `JSON.stringify({})` alone: a selective wrapper returns `{}` for that and still refuses the shape that produced this line. Serialize a record of the shape the RETRY formats - the diagnostic envelope with no attributes, every field a bounded string or number - and compare that with the trivial case; a difference between them IS the wrapper. Do not test the event's own attributes: an attribute that cannot be serialized is absorbed by the retry and never produces this line, so a throw from serializing them proves nothing about this failure and reads as a wrapper that is not there. The event name on the line is what was in flight, not a suspect.",
		sources: Object.freeze(['src/runtime/diagnostic.js']),
		anchor: 'adapter-err-diagnostic-render-collapse',
		help: 'docs/errors.md#adapter-err-diagnostic-render-collapse'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.DIAGNOSTIC_CONSOLE_WRITE,
		code: null,
		event: 'ws.diagnostic.console-write',
		component: null,
		severity: 'error',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[ws] diagnostic rendered but the console refused it: ',
		cause: 'The diagnostic formatted correctly and the console method carrying its severity threw when handed the finished line. The record is not the suspect here: a console replaced or wrapped by the host, or one whose write end has gone, refuses a well-formed string exactly the same way.',
		consequence: 'That one diagnostic is lost, and so is every later one carried by the SAME console method for as long as it keeps throwing - this is a broken channel rather than a bad value, so it does not stop at the record that revealed it. The mapping is not one method per severity: debug, info, and warn ride their own methods, while error and fatal share console.error, so a broken error channel loses both and a fatal line missing from the log means console.error is broken. Severities riding other methods are unaffected, and this line itself is written through console.error, which is a different method in every case but a failing error or fatal channel.',
		automaticRecovery: 'None, and none is attempted: nothing re-routes a severity to another method, because silently moving warnings into the error stream would corrupt the log an operator reads.',
		nextAction: 'Look at the console rather than the emitter - specifically anything in the deployment that replaces, wraps, or proxies it (a log shipper, an APM agent, a test harness stub). The event name on the line says what was being reported when it went; the severity that is missing from the log tells you which method is broken. If nothing wraps the console, check whether its destination still exists - a closed pipe or a full stream refuses writes the same way.',
		sources: Object.freeze(['src/runtime/diagnostic.js']),
		anchor: 'adapter-err-diagnostic-console-write',
		help: 'docs/errors.md#adapter-err-diagnostic-console-write'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.DIAGNOSTIC_SINK_NOTICE,
		code: null,
		event: 'ws.diagnostic.sink-notice',
		component: null,
		severity: 'error',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[ws] operational sink failed and its failure notice could not be built: ',
		cause: 'A configured operational event sink threw, the console fallback printed the original event in its place, and then BUILDING the record that announces the sink failure threw. Only the wall clock the notice stamps can do that: every other field is a constant or a value read off the already-validated original record, and a clock either throws or returns a timestamp the shape accepts - no clock RETURN value can be rejected. The console is not implicated: this very line is written through it.',
		consequence: 'One record is lost, and it is the notice, not the event. The event named on this line was printed by the console fallback immediately above it, so the telemetry the sink was carrying is in the log; what is missing is the machine-readable statement that the sink is broken, which is what a collector watching for sink health would have keyed on.',
		automaticRecovery: "None for the lost notice. The sink is not unregistered and is called again for the next event. This line is inherently intermittent: it needs the same clock to succeed for the event's own record and then fail for the notice moments later, so a clock broken outright does not keep printing it - the next emission dies earlier, at record construction, and prints the record-shape line instead.",
		nextAction: 'Two independent things failed and both are worth a look. The sink is the deployment-supplied component that failed first, and the event name on the line says what it was carrying. The notice failure is separate and is the adapter-side wall clock - an injected clock that throws intermittently, or an async sink rejection settling after the clock broke. A deployment that has not injected a clock should treat this half as a defect worth reporting.',
		sources: Object.freeze(['src/runtime/diagnostic.js']),
		anchor: 'adapter-err-diagnostic-sink-notice',
		help: 'docs/errors.md#adapter-err-diagnostic-sink-notice'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.METRICS_MODULE_SHAPE,
		code: null,
		event: 'ws.metrics.module-shape',
		component: null,
		severity: 'error',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[ws] the metrics module must export a registry object as `default`, `metrics` or `registry`; got ',
		cause: 'The export the build SELECTED from the module named by `websocket.metrics` cannot carry instrument factories. This is a statement about that one value, not about the module: the build takes the first of `default`, `metrics` and `registry` that is not nullish and forwards it without validating its shape, so a module carrying a perfectly good `metrics` registry prints this line whenever a primitive `default` sits in front of it.',
		consequence: 'Metrics are disabled for the whole worker: no instrument is ever created, so the adapter series are ABSENT from the scrape rather than present at zero. Dashboards read as no data and alerts that fire on a threshold never fire at all.',
		automaticRecovery: 'None. The runtime keeps serving traffic with metrics off - the alternative is a boot failure naming neither metrics nor the option that caused it.',
		nextAction: 'The guard accepts any object or function, so what printed this is a PRIMITIVE - the `got` value on the line says which type. Export the registry itself under any one of the three names the build reads, which it tries in order: `default`, then `metrics`, then `registry`. The first one that is not nullish wins, so a primitive `default` masks a perfectly good named `metrics` beside it and is worth ruling out first.',
		sources: Object.freeze(['src/runtime/utils/metrics.js']),
		anchor: 'adapter-err-metrics-module-shape',
		help: 'docs/errors.md#adapter-err-metrics-module-shape'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.METRICS_INSTRUMENT,
		code: null,
		event: 'ws.metrics.instrument-threw',
		component: null,
		severity: 'error',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[ws] a metrics instrument threw; further errors from it are suppressed',
		cause: "Recording a value threw. The containment wraps the adapter's own mirror of the instrument, so the throw is usually from the registry module the `websocket.metrics` option names, but adapter code runs first on that path and a failure there surfaces the same way.",
		consequence: "Where the value ends up depends on which side threw. The adapter records into its own mirror BEFORE delegating, so a throw from the configured registry loses the value only from that registry's scrape - `metricsSnapshot()` still has it. Every later call is attempted again, the containment being per call, so an instrument that throws for one label set or one transient keeps recording the rest; but the failures are printed once and then suppressed, so nothing tells you whether it kept failing. A series that stops moving reads as an idle server rather than a broken instrument.",
		automaticRecovery: 'The throw is contained per call, so the request or frame that triggered it completes normally, and a transient failure self-heals on the next call.',
		nextAction: 'Read the error printed with this line - it is the original throw, and its stack says which side failed. Label cardinality and type mismatches in the registry module are the usual causes.',
		sources: Object.freeze(['src/runtime/utils/metrics.js']),
		anchor: 'adapter-err-metrics-instrument',
		help: 'docs/errors.md#adapter-err-metrics-instrument'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.POSTURE_EXPORT_DISABLED,
		code: null,
		event: 'ws.posture-export.disabled',
		component: null,
		severity: 'warn',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[ws] posture export disabled: ',
		cause: 'The posture export socket could not listen on its configured path, or its socket failed later in its life; the line says which. A stale socket file is removed automatically before every listen attempt, so the listen shape means a permission denial (including a stale path the process could not remove), a missing parent directory, or a Windows named pipe already taken.',
		consequence: 'The worker keeps serving traffic, but nothing can read its live pressure posture over that socket: an external supervisor watching it sees a connection failure rather than a posture, and any shedding decision built on it stops updating. A failed listen never had readers to lose; a later socket error drops whichever readers were connected.',
		automaticRecovery: 'None. The export is not retried for the life of the worker.',
		nextAction: 'Read the shape on the line. For a failed listen, fix the directory permissions, create the missing parent directory, or point the export at a free path, then restart the worker - removing a stale socket file by hand is not the repair, because the runtime already removes one before every listen. For a later socket error the export is down until restart; its consumers key on the 1 Hz cadence stopping either way.',
		sources: Object.freeze(['src/runtime/utils/posture-export.js']),
		anchor: 'adapter-err-posture-export-disabled',
		help: 'docs/errors.md#adapter-err-posture-export-disabled'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.UPGRADE_DEFERRED,
		code: null,
		event: 'ws.upgrade.deferred-failed',
		component: null,
		severity: 'error',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[ws] a deferred upgrade failed',
		cause: 'A WebSocket upgrade held back by the admission queue threw when it was finally completed, after the client had already passed admission.',
		consequence: 'That one client never connects. Its response is left unfinished rather than refused, so it typically waits out its own timeout instead of seeing an error, and it retries as if the server were briefly unavailable. The admission slot it held is released, and the other upgrades in the same drain still run.',
		automaticRecovery: 'None for that connection; the client reconnects on its own schedule.',
		nextAction: 'Read the error printed with this line. Its source cannot be your upgrade hook - the hook had already resolved before the completion was deferred; what runs here is the socket write, the upgrade handshake, the permit bookkeeping, and, on the fast path, the tracing hook. A repeated throw usually means sockets are dying in the queue before their turn comes - the admission backlog is holding upgrades longer than clients wait.',
		sources: Object.freeze(['src/runtime/utils/upgrade-admission.js']),
		anchor: 'adapter-err-upgrade-deferred',
		help: 'docs/errors.md#adapter-err-upgrade-deferred'
	}),
	Object.freeze({
		id: ADAPTER_ERROR_IDS.WAITING_ROOM_FALLBACK,
		code: null,
		event: 'ws.waiting-room.renderer-failed',
		component: null,
		severity: 'error',
		emission: 'console',
		problemPrefix: null,
		messagePrefix: '[svelte-adapter-ws] the waiting-room renderer failed; serving the built-in English page instead',
		cause: 'The module named by `waitingRoom.renderer` threw, or returned a result the runtime could not accept, while rendering the over-capacity page.',
		consequence: 'The visitor turned away by that request receives the built-in English page instead of the rendered one, so localization, branding and any per-request content are lost for it. The capacity decision itself is unaffected. The line prints once per worker, so a renderer that keeps failing reports only the first one.',
		automaticRecovery: 'The renderer is called again on the next request, so a failure that depends on the request self-heals; a renderer that always throws serves the built-in page every time.',
		nextAction: "Fix the renderer and redeploy. A renderer must return a complete document meeting the accessible baseline, which is validated on the first successful render rather than at build time; when the baseline is what failed the error printed with this line names the requirement, and for any other throw it is the renderer's own error.",
		sources: Object.freeze(['src/runtime/utils/upgrade-admission.js']),
		anchor: 'adapter-err-waiting-room-fallback',
		help: 'docs/errors.md#adapter-err-waiting-room-fallback'
	})
]);

const ERROR_BY_ID = new Map(ADAPTER_ERROR_REGISTRY.map((entry) => [entry.id, entry]));

export function adapterErrorDefinition(id) {
	const entry = ERROR_BY_ID.get(id);
	if (!entry) throw new TypeError('Unknown svelte-adapter-ws error id: ' + id);
	return entry;
}

export function adapterErrorHelpSuffix(id) {
	const entry = adapterErrorDefinition(id);
	// A console or an Error message cannot resolve the repo-relative docs
	// route, so only the stable id (which finds the entry anywhere) and an
	// absolute shortlink, when the entry has one, are appended.
	return ' [' + entry.id + ']' + (entry.link ? ' See: ' + entry.link : '');
}

/**
 * Rejection details for ADAPTER-ERR-REQUEST-CLOSED, shared by every runtime
 * twin so the three transmission-side statements cannot drift apart. The
 * close sweep picks by the recorded send outcome: a frame the transport
 * dropped - or that a non-open dev socket never carried - is NEVER_SENT even
 * though the caller's send call returned, and UNANSWERED deliberately claims
 * a hand-off to the transport rather than delivery to the peer, which is all
 * a returned send can stand behind.
 */
export const REQUEST_CLOSED_DETAIL = Object.freeze({
	NEVER_SENT: '; the request frame was never sent',
	SEND_FAILED: '; the request frame could not be sent',
	UNANSWERED: '; the request frame was handed to the transport and no reply had arrived'
});

export function adapterErrorMessage(id, detail = '') {
	const entry = adapterErrorDefinition(id);
	return entry.messagePrefix + detail + adapterErrorHelpSuffix(id);
}

export function adapterErrorProblem(id, detail = '') {
	const entry = adapterErrorDefinition(id);
	if (entry.problemPrefix === null) throw new TypeError('Adapter error id has no operational problem prefix: ' + id);
	return entry.problemPrefix + detail + adapterErrorHelpSuffix(id);
}

/**
 * The full text of a console-emitted failure line: the registry's prefix, the
 * call-site detail, the stable ID tag, and the shortlink when the entry has
 * one. Printing THROUGH the registry is what keeps the emitted line and the
 * indexed prefix the same bytes - a call site cannot reword one without the
 * other. No repo-relative help route is appended: a console cannot resolve
 * one, and the stable ID already finds the entry.
 */
export function adapterConsoleLine(id, detail = '') {
	const entry = adapterErrorDefinition(id);
	if (entry.emission !== 'console') throw new TypeError('Adapter error id is not console-emitted: ' + id);
	return entry.messagePrefix + detail + ' [' + entry.id + ']' + (entry.link ? ' See: ' + entry.link : '');
}
