[
	{
		"file": "scripts/sim-git-commit.js"
	},
	{
		"file": "src/client-runtime.js"
	},
	{
		"file": "src/client.d.ts"
	},
	{
		"file": "src/client.js"
	},
	{
		"file": "src/config-guards.js"
	},
	{
		"file": "src/dev-dashboard.js",
		"adaptations": [
			{
				"lead": "Symbol.for('svelte-adapter-ws.dashboard-contributors')",
				"ours": "Symbol.for('svelte-adapter-uws.dashboard-contributors')"
			}
		]
	},
	{
		"file": "src/observability.d.ts"
	},
	{
		"file": "src/observability.generated.d.ts"
	},
	{
		"file": "src/observability.js"
	},
	{
		"file": "src/plugins/_shared/sensitive.js"
	},
	{
		"file": "src/plugins/channels/client.d.ts"
	},
	{
		"file": "src/plugins/channels/client.js"
	},
	{
		"file": "src/plugins/channels/server.d.ts",
		"adaptations": [
			{
				"lead": "import type { WebSocket } from 'uWebSockets.js';\n",
				"ours": ""
			},
			{
				"lead": "WebSocket<any>",
				"ours": "object",
				"all": true
			}
		]
	},
	{
		"file": "src/plugins/channels/server.js"
	},
	{
		"file": "src/plugins/crdt/channel.d.ts"
	},
	{
		"file": "src/plugins/crdt/channel.js"
	},
	{
		"file": "src/plugins/crdt/client.d.ts"
	},
	{
		"file": "src/plugins/crdt/client.js"
	},
	{
		"file": "src/plugins/crdt/codec.js",
		"adaptations": [
			{
				"lead": "(e.g. `svelte-adapter-ws-extensions/redis/crdt`)",
				"ours": "(e.g. `svelte-adapter-uws-extensions/redis/crdt`)"
			}
		]
	},
	{
		"file": "src/plugins/crdt/replica.d.ts"
	},
	{
		"file": "src/plugins/crdt/replica.js"
	},
	{
		"file": "src/plugins/crdt/server.d.ts",
		"adaptations": [
			{
				"lead": "(`svelte-adapter-ws-extensions/redis/crdt`) builds",
				"ours": "(`svelte-adapter-uws-extensions/redis/crdt`) builds"
			}
		]
	},
	{
		"file": "src/plugins/crdt/server.js",
		"adaptations": [
			{
				"lead": "(`svelte-adapter-ws-extensions/redis/crdt`) so the in-memory",
				"ours": "(`svelte-adapter-uws-extensions/redis/crdt`) so the in-memory"
			}
		]
	},
	{
		"file": "src/plugins/cursor/client.d.ts"
	},
	{
		"file": "src/plugins/cursor/client.js"
	},
	{
		"file": "src/plugins/cursor/codec.js"
	},
	{
		"file": "src/plugins/cursor/cursor-worker.js"
	},
	{
		"file": "src/plugins/cursor/decode.d.ts"
	},
	{
		"file": "src/plugins/cursor/decode.js"
	},
	{
		"file": "src/plugins/cursor/render/canvas2d.js"
	},
	{
		"file": "src/plugins/cursor/render/index.js"
	},
	{
		"file": "src/plugins/cursor/render/webgl2.js"
	},
	{
		"file": "src/plugins/cursor/render/webgpu.js"
	},
	{
		"file": "src/plugins/cursor/server.d.ts",
		"adaptations": [
			{
				"lead": " * `svelte-adapter-ws-extensions/redis/cursor`) builds the IDENTICAL codec from",
				"ours": " * `svelte-adapter-uws-extensions/redis/cursor`) builds the IDENTICAL codec from"
			},
			{
				"lead": "import type { WebSocket } from 'uWebSockets.js';\n",
				"ours": ""
			},
			{
				"lead": "WebSocket<any>",
				"ours": "object",
				"all": true
			}
		]
	},
	{
		"file": "src/plugins/cursor/server.js",
		"adaptations": [
			{
				"lead": "// (svelte-adapter-ws-extensions redis/cursor) builds the identical codec.",
				"ours": "// (svelte-adapter-uws-extensions redis/cursor) builds the identical codec."
			},
			{
				"lead": " * dictionary). Exported so the cluster-backed variant (svelte-adapter-ws-",
				"ours": " * dictionary). Exported so the cluster-backed variant (svelte-adapter-uws-"
			}
		]
	},
	{
		"file": "src/plugins/dedup/server.d.ts"
	},
	{
		"file": "src/plugins/dedup/server.js"
	},
	{
		"file": "src/plugins/groups/client.d.ts"
	},
	{
		"file": "src/plugins/groups/client.js"
	},
	{
		"file": "src/plugins/groups/server.d.ts",
		"adaptations": [
			{
				"lead": "import type { WebSocket } from 'uWebSockets.js';\n",
				"ours": ""
			},
			{
				"lead": "WebSocket<any>",
				"ours": "object",
				"all": true
			}
		]
	},
	{
		"file": "src/plugins/groups/server.js"
	},
	{
		"file": "src/plugins/lock/server.d.ts"
	},
	{
		"file": "src/plugins/lock/server.js"
	},
	{
		"file": "src/plugins/middleware/server.d.ts",
		"adaptations": [
			{
				"lead": "import type { Platform } from '../../index.js';\nimport type { WebSocket } from 'uWebSockets.js';",
				"ours": "import type { Platform } from '../../index.js';"
			},
			{
				"lead": "\tws: WebSocket<any>;",
				"ours": "\tws: object;"
			},
			{
				"lead": "\t\tws: WebSocket<any>,",
				"ours": "\t\tws: object,"
			}
		]
	},
	{
		"file": "src/plugins/middleware/server.js"
	},
	{
		"file": "src/plugins/presence/client.d.ts"
	},
	{
		"file": "src/plugins/presence/client.js",
		"adaptations": [
			{
				"lead": "in svelte-adapter-ws-extensions) emits",
				"ours": "in svelte-adapter-uws-extensions) emits"
			}
		]
	},
	{
		"file": "src/plugins/presence/codec.js"
	},
	{
		"file": "src/plugins/presence/server.d.ts",
		"adaptations": [
			{
				"lead": "`svelte-adapter-ws-extensions/redis/presence`)",
				"ours": "`svelte-adapter-uws-extensions/redis/presence`)"
			},
			{
				"lead": "import type { WebSocket } from 'uWebSockets.js';\n",
				"ours": ""
			},
			{
				"lead": "WebSocket<any>",
				"ours": "object",
				"all": true
			}
		]
	},
	{
		"file": "src/plugins/presence/server.js",
		"adaptations": [
			{
				"lead": "variant in svelte-adapter-ws-extensions.",
				"ours": "variant in svelte-adapter-uws-extensions."
			},
			{
				"lead": "(`svelte-adapter-ws-extensions`",
				"ours": "(`svelte-adapter-uws-extensions`"
			}
		]
	},
	{
		"file": "src/plugins/queue/server.d.ts"
	},
	{
		"file": "src/plugins/queue/server.js"
	},
	{
		"file": "src/plugins/ratelimit/server.d.ts",
		"adaptations": [
			{
				"lead": "\tkeyBy?: 'ip' | 'connection' | ((ws: import('uWebSockets.js').WebSocket<UserData>) => string);",
				"ours": "\tkeyBy?: 'ip' | 'connection' | ((ws: object) => string);"
			},
			{
				"lead": "\ttenant?: (ws: import('uWebSockets.js').WebSocket<UserData>) => string | null | undefined;",
				"ours": "\ttenant?: (ws: object) => string | null | undefined;"
			},
			{
				"lead": "\tconsume(ws: import('uWebSockets.js').WebSocket<any>, cost?: number): ConsumeResult;",
				"ours": "\tconsume(ws: object, cost?: number): ConsumeResult;"
			}
		]
	},
	{
		"file": "src/plugins/ratelimit/server.js",
		"adaptations": [
			{
				"lead": "\t// budget: getUserData() is a native uWS call, and paying it per consume",
				"ours": "\t// budget: in the lead uWS adapter getUserData() is a native call, and paying it per consume"
			}
		]
	},
	{
		"file": "src/plugins/replay/client.d.ts"
	},
	{
		"file": "src/plugins/replay/client.js"
	},
	{
		"file": "src/plugins/replay/server.d.ts",
		"adaptations": [
			{
				"lead": "import type { WebSocket } from 'uWebSockets.js';\n",
				"ours": ""
			},
			{
				"lead": "WebSocket<any>",
				"ours": "object",
				"all": true
			}
		]
	},
	{
		"file": "src/plugins/replay/server.js",
		"adaptations": [
			{
				"lead": " * svelte-adapter-ws-extensions is the clustered form.",
				"ours": " * svelte-adapter-uws-extensions is the clustered form."
			},
			{
				"lead": "'shared-backend replay from svelte-adapter-ws-extensions, or run a ' +",
				"ours": "'shared-backend replay from svelte-adapter-uws-extensions, or run a ' +"
			}
		]
	},
	{
		"file": "src/plugins/session/server.d.ts"
	},
	{
		"file": "src/plugins/session/server.js"
	},
	{
		"file": "src/plugins/smooth/cell-codec.js"
	},
	{
		"file": "src/plugins/smooth/client.d.ts"
	},
	{
		"file": "src/plugins/smooth/client.js",
		"adaptations": [
			{
				"lead": "export const SMOOTH_FRESHNESS = Symbol.for('svelte-adapter-ws.smooth.freshness');",
				"ours": "export const SMOOTH_FRESHNESS = Symbol.for('svelte-adapter-uws.smooth.freshness');"
			}
		]
	},
	{
		"file": "src/plugins/smooth/clock.js"
	},
	{
		"file": "src/plugins/smooth/codec.js"
	},
	{
		"file": "src/plugins/smooth/interpolate.js"
	},
	{
		"file": "src/plugins/smooth/predict.js"
	},
	{
		"file": "src/plugins/smooth/random.d.ts"
	},
	{
		"file": "src/plugins/smooth/random.js"
	},
	{
		"file": "src/plugins/smooth/server.d.ts"
	},
	{
		"file": "src/plugins/smooth/server.js"
	},
	{
		"file": "src/plugins/throttle/server.d.ts"
	},
	{
		"file": "src/plugins/throttle/server.js"
	},
	{
		"file": "src/plugins/webhooks/controls.js"
	},
	{
		"file": "src/plugins/webhooks/server.d.ts"
	},
	{
		"file": "src/plugins/webhooks/server.js"
	},
	{
		"file": "src/runtime/audit-snapshot.js"
	},
	{
		"file": "src/runtime/auditor.js"
	},
	{
		"file": "src/runtime/diagnostic-format.js"
	},
	{
		"file": "src/runtime/diagnostic.js",
		"adaptations": [
			{
				"lead": "\t['extensions', ['svelte-adapter-ws-extensions', 'runtime.extensions', 'diagnostic.legacy', 'warn']],\n\t['extensions/assert', ['svelte-adapter-ws-extensions', 'runtime.assertion', 'invariant.violated', 'warn']],\n\t['extensions/fatal', ['svelte-adapter-ws-extensions', 'runtime.assertion', 'invariant.violated', 'fatal']],\n\t['extensions/devAssert', ['svelte-adapter-ws-extensions', 'runtime.assertion', 'invariant.violated', 'error']],\n\t['redis', ['svelte-adapter-ws-extensions', 'runtime.redis', 'diagnostic.legacy', 'warn']],\n\t['redis replay', ['svelte-adapter-ws-extensions', 'runtime.redis.replay', 'diagnostic.legacy', 'warn']],\n\t['redis stream replay', ['svelte-adapter-ws-extensions', 'runtime.redis.stream-replay', 'diagnostic.legacy', 'warn']],\n\t['postgres', ['svelte-adapter-ws-extensions', 'runtime.postgres', 'diagnostic.legacy', 'warn']],\n\t['postgres replay', ['svelte-adapter-ws-extensions', 'runtime.postgres.replay', 'diagnostic.legacy', 'warn']],\n\t['postgres tasks', ['svelte-adapter-ws-extensions', 'runtime.postgres.tasks', 'diagnostic.legacy', 'warn']],\n\t['publish-rate', ['svelte-adapter-ws-extensions', 'runtime.redis.publish-rate', 'diagnostic.legacy', 'warn']],",
				"ours": "\t['extensions', ['svelte-adapter-uws-extensions', 'runtime.extensions', 'diagnostic.legacy', 'warn']],\n\t['extensions/assert', ['svelte-adapter-uws-extensions', 'runtime.assertion', 'invariant.violated', 'warn']],\n\t['extensions/fatal', ['svelte-adapter-uws-extensions', 'runtime.assertion', 'invariant.violated', 'fatal']],\n\t['extensions/devAssert', ['svelte-adapter-uws-extensions', 'runtime.assertion', 'invariant.violated', 'error']],\n\t['redis', ['svelte-adapter-uws-extensions', 'runtime.redis', 'diagnostic.legacy', 'warn']],\n\t['redis replay', ['svelte-adapter-uws-extensions', 'runtime.redis.replay', 'diagnostic.legacy', 'warn']],\n\t['redis stream replay', ['svelte-adapter-uws-extensions', 'runtime.redis.stream-replay', 'diagnostic.legacy', 'warn']],\n\t['postgres', ['svelte-adapter-uws-extensions', 'runtime.postgres', 'diagnostic.legacy', 'warn']],\n\t['postgres replay', ['svelte-adapter-uws-extensions', 'runtime.postgres.replay', 'diagnostic.legacy', 'warn']],\n\t['postgres tasks', ['svelte-adapter-uws-extensions', 'runtime.postgres.tasks', 'diagnostic.legacy', 'warn']],\n\t['publish-rate', ['svelte-adapter-uws-extensions', 'runtime.redis.publish-rate', 'diagnostic.legacy', 'warn']],"
			},
			{
				"lead": "\t\tfields = ['svelte-adapter-ws-extensions', `runtime.${match[1].replace('/', '.')}`, 'diagnostic.legacy', defaultSeverity];",
				"ours": "\t\tfields = ['svelte-adapter-uws-extensions', `runtime.${match[1].replace('/', '.')}`, 'diagnostic.legacy', defaultSeverity];"
			}
		]
	},
	{
		"file": "src/runtime/divergence-diagnostics.js"
	},
	{
		"file": "src/runtime/handler/cohort.js"
	},
	{
		"file": "src/runtime/handler/egress-budget.js"
	},
	{
		"file": "src/runtime/handler/metrics-snapshot.js"
	},
	{
		"file": "src/runtime/handler/pressure-metrics.js"
	},
	{
		"file": "src/runtime/handler/shared-wire-id.js"
	},
	{
		"file": "src/runtime/invariants.js",
		"adaptations": [
			{
				"lead": "(see handler/state.js `recordOriginStream`)",
				"ours": "(see the lead adapter's handler/state.js `recordOriginStream`)"
			}
		]
	},
	{
		"file": "src/runtime/leak-detect.js"
	},
	{
		"file": "src/runtime/leak-probes.js"
	},
	{
		"file": "src/runtime/metrics-bridge.js"
	},
	{
		"file": "src/runtime/metrics-collector.js"
	},
	{
		"file": "src/runtime/observability-manifest.js"
	},
	{
		"file": "src/runtime/posture-collector.js"
	},
	{
		"file": "src/runtime/sim-cluster.js",
		"adaptations": [
			{
				"lead": "(handler.js imports uWS + build-virtual modules at module scope",
				"ours": "(handler.js imports node:http + build-virtual modules at module scope"
			}
		]
	},
	{
		"file": "src/runtime/sim-core.js",
		"adaptations": [
			{
				"lead": "real uWS in createTestServer",
				"ours": "real ws in createTestServer"
			}
		]
	},
	{
		"file": "src/runtime/state-hash-detector.js"
	},
	{
		"file": "src/runtime/steadystate.js"
	},
	{
		"file": "src/runtime/transport-metrics.js"
	},
	{
		"file": "src/runtime/utils/address.js",
		"adaptations": [
			{
				"lead": " * IPv4-in-IPv6 to defeat smuggling - is the canonical\n * `svelte-adapter-ws/safe-url`, and there is intentionally one copy of that",
				"ours": " * IPv4-in-IPv6 to defeat smuggling - is\n * `svelte-adapter-ws/safe-url`, and there is intentionally one copy of that"
			},
			{
				"lead": " * back (`getRemoteAddressAsText`), which is OS-produced and never",
				"ours": " * back (`socket.remoteAddress`), which is OS-produced and never"
			}
		]
	},
	{
		"file": "src/runtime/utils/chaos.js"
	},
	{
		"file": "src/runtime/utils/connection-permit.js"
	},
	{
		"file": "src/runtime/utils/cookies-string.js"
	},
	{
		"file": "src/runtime/utils/egress-account.js"
	},
	{
		"file": "src/runtime/utils/epoch.js"
	},
	{
		"file": "src/runtime/utils/metrics-merge.js"
	},
	{
		"file": "src/runtime/utils/metrics.js"
	},
	{
		"file": "src/runtime/utils/observability-privacy.js"
	},
	{
		"file": "src/runtime/utils/operational-diagnostic.js",
		"adaptations": [
			{
				"lead": "new Error('uWebSockets.js returned no listen socket')",
				"ours": "new Error('no listen socket was bound')"
			}
		]
	},
	{
		"file": "src/runtime/utils/posture-export.js"
	},
	{
		"file": "src/runtime/utils/pressure.js"
	},
	{
		"file": "src/runtime/utils/static-headers.js"
	},
	{
		"file": "src/runtime/utils/subscribe-policy.js"
	},
	{
		"file": "src/runtime/utils/upgrade-admission.js"
	},
	{
		"file": "src/runtime/utils/waiting-room-template.js"
	},
	{
		"file": "src/runtime/waiting-room-renderer-bridge.js"
	},
	{
		"file": "src/safe-url.d.ts"
	},
	{
		"file": "src/safe-url.js",
		"adaptations": [
			{
				"lead": " * This is the single canonical copy for the ecosystem: it lives here in the\n * adapter (the layer every package depends on) and is re-exported by\n * `svelte-adapter-ws-extensions/safe-url`; `svelte-realtime` imports it\n * directly. There is intentionally no second copy to drift.",
				"ours": " * The ecosystem's canonical copy lives in svelte-adapter-uws (the lead\n * adapter); this package carries the same validator so `./safe-url` resolves\n * identically whichever adapter an app installs. The family conformance\n * suite holds the two in lockstep."
			}
		]
	},
	{
		"file": "src/sim.d.ts"
	},
	{
		"file": "src/sim.js",
		"adaptations": [
			{
				"lead": "// that runs over real uWS, but over an in-memory app under a virtual clock and a\n",
				"ours": "// that runs over the real node:http + ws transport, but over an in-memory app\n// under a virtual clock and a\n"
			}
		]
	},
	{
		"file": "src/testing.d.ts",
		"adaptations": [
			{
				"lead": "import type { WebSocket } from 'uWebSockets.js';\n",
				"ours": ""
			},
			{
				"lead": "\t * real uWS socket AND reported by `platform.maxPayloadLength` from the\n",
				"ours": "\t * real ws receiver AND reported by `platform.maxPayloadLength` from the\n"
			},
			{
				"lead": "\t * Idle timeout in seconds applied by the harness's real uWS socket,\n\t * mirroring the production `websocket.idleTimeout` option with the same\n\t * guard and the same pass-through: values reach uWS unclamped, and uWS\n\t * applies its own coarse (roughly 4-second) timeout grid and refuses very\n\t * low nonzero values at socket setup. `0` disables the idle reaper\n\t * entirely. A test that wants to observe an idle close should use `8`.\n",
				"ours": "\t * Idle timeout in seconds applied by the harness's ping/pong idle reaper,\n\t * mirroring the production runtime's own: a live peer answers the\n\t * half-interval ping and the deadline never fires; a vanished peer is\n\t * terminated at the timeout. `0` disables the idle reaper entirely. A\n\t * test that wants to observe an idle close should use `8`.\n"
			},
			{
				"lead": "\t * Live set of currently connected uWS WebSocket instances. Useful in\n",
				"ours": "\t * Live set of currently connected server-side socket handles. Useful in\n"
			},
			{
				"lead": "\twsConnections: Set<import('uWebSockets.js').WebSocket<any>>;\n",
				"ours": "\twsConnections: Set<any>;\n"
			},
			{
				"lead": " * Create a lightweight test server backed by a real uWebSockets.js instance.\n",
				"ours": " * Create a lightweight test server backed by a real node:http + ws server.\n"
			},
			{
				"lead": "// `WS_REQUEST_ID_KEY` is a string (not a Symbol): uWebSockets.js strips\n// Symbol-keyed properties from the userData object passed to\n// `res.upgrade()`, so the upgrade->open carrier slot has to be a string\n// key. The `open` hook moves the value into the Symbol-keyed\n// `WS_PLATFORM` slot and deletes the string key, so test code reading\n// userData after `open` should read `WS_PLATFORM`, not the string key.\n",
				"ours": "// `WS_REQUEST_ID_KEY` is a string (not a Symbol), the family harness's\n// upgrade->open carrier shape. The `open` hook moves the value into the\n// Symbol-keyed `WS_PLATFORM` slot and deletes the string key, so test\n// code reading userData after `open` should read `WS_PLATFORM`, not the\n// string key.\n"
			},
			{
				"lead": " * across the upgrade->open transition. uWebSockets.js strips Symbol keys\n * from the userData object handed to `res.upgrade()`, so the carrier has\n * to be a string. The `open` hook deletes this slot after promoting the\n * value into the `WS_PLATFORM` Symbol slot, so it never appears in\n * userData while a hook is running.\n",
				"ours": " * across the upgrade->open transition, the family harness's carrier\n * shape. The `open` hook deletes this slot after promoting the value\n * into the `WS_PLATFORM` Symbol slot, so it never appears in userData\n * while a hook is running.\n"
			},
			{
				"lead": "export const WS_REQUEST_ID_KEY: '__adapter_uws_request_id__';\n",
				"ours": "export const WS_REQUEST_ID_KEY: '__adapter_ws_request_id__';\n"
			}
		]
	},
	{
		"file": "src/vite.d.ts"
	},
	{
		"file": "src/vite.js",
		"adaptations": [
			{
				"lead": "WS_PLATFORM, WS_REQUEST_ID_KEY, WS_CAPS",
				"ours": "WS_PLATFORM, WS_CAPS"
			},
			{
				"lead": "import { ADAPTER_ERROR_IDS, REQUEST_CLOSED_DETAIL, adapterConsoleLine, adapterErrorMessage } from './runtime/error-registry.js';\n",
				"ours": "import { ADAPTER_ERROR_IDS, REQUEST_CLOSED_DETAIL, adapterConsoleLine, adapterErrorMessage } from './runtime/error-registry.js';\n\n// The per-connection requestId hand-off slot, set in the upgrade callback\n// and promoted-then-deleted in the connection handler before any app hook\n// reads userData. Module-local: no other surface reads it, and this\n// runtime has no boundary that would strip a Symbol key anyway.\nconst WS_REQUEST_ID_KEY = '__adapter_uws_request_id__';\n"
			},
			{
				"lead": " * Uses the same subscribe/unsubscribe/publish protocol as the production\n * uWS handler, so the client store works identically in dev and prod.",
				"ours": " * Uses the same subscribe/unsubscribe/publish protocol as the production\n * handler, so the client store works identically in dev and prod."
			},
			{
				"lead": "exactly as uWS\n\t\t// does on the production surface.",
				"ours": "in dev exactly\n\t\t// as on the production surface."
			},
			{
				"lead": "copy sitting after the call, and testing.js had a third spelling.",
				"ours": "copy sitting after the call, and the lead adapter's testing.js had a third spelling."
			},
			{
				"lead": "against `createTestServer` (testing.js).",
				"ours": "against `createTestServer` (the lead adapter's testing.js)."
			},
			{
				"lead": "ships and is tested in production (src/runtime/handler.js) and the test\n\t\t// server (testing.js).",
				"ours": "ships and is tested in production (src/runtime/handler.js) and the test\n\t\t// server (the lead adapter's testing.js)."
			},
			{
				"lead": "(handler/platform.js) and the in-process server (testing.js) both",
				"ours": "(handler/platform.js) and the in-process server (the lead adapter's\n\t\t\t// testing.js) both"
			},
			{
				"lead": "Mirrors handler.js + testing.js.",
				"ours": "Mirrors handler.js + the lead adapter's testing.js."
			},
			{
				"lead": "Production and src/testing.js both",
				"ours": "Production and the lead adapter's src/testing.js both"
			},
			{
				"lead": "// not granted. Dev's `ws.getUserData()` never throws (Node ws, not uWS),\n\t\t// so there is no closed-socket abort path here.",
				"ours": "// not granted. Dev's `ws.getUserData()` never throws (the wrapper closes\n\t\t// over its userData), so there is no closed-socket abort path here."
			},
			{
				"lead": "\t\t\t// Dev uses Node's `ws` library, not uWS - sockets do not\n\t\t\t// throw \"Invalid access\" when written to after close, so\n\t\t\t// the closed-WS abort path doesn't exist here. Mirror the\n\t\t\t// prod surface as a constant zero.",
				"ours": "\t\t\t// Dev writes on bare `ws` sockets, which do not throw when\n\t\t\t// written to after close, so the closed-WS abort path doesn't\n\t\t\t// exist here. Mirror the prod surface as a constant zero."
			},
			{
				"lead": "adapter?.name !== 'adapter-uws'",
				"ours": "adapter?.name !== 'adapter-ws'",
				"all": true
			},
			{
				"lead": "[adapter-uws]",
				"ours": "[adapter-ws]",
				"all": true
			}
		]
	},
	{
		"file": "test-vectors/README.md"
	},
	{
		"file": "test-vectors/binary.json"
	},
	{
		"file": "test-vectors/frames.json"
	},
	{
		"file": "test-vectors/game-fanout-compact.json"
	},
	{
		"file": "test-vectors/game-relay.json"
	},
	{
		"file": "test-vectors/webtransport-stream.json"
	},
	{
		"file": "test/_helpers.js",
		"adaptations": [
			{
				"lead": " * Plugin servers expect a uWS / vite wrapper-shaped WebSocket and a",
				"ours": " * Plugin servers expect a ws facade / vite wrapper-shaped WebSocket and a"
			},
			{
				"lead": " * Create a mock WebSocket that mimics the uWS / vite wrapper API.",
				"ours": " * Create a mock WebSocket that mimics the ws facade / vite wrapper API."
			}
		]
	},
	{
		"file": "test/admin-route.test.js",
		"adaptations": [
			{
				"lead": "// The reserved `/__realtime/*` admin route is wired over a real uWS server.\n// Skip the whole suite when the optional uWebSockets.js binding is absent.\nlet uWS;\ntry {\n\tuWS = (await import('uWebSockets.js')).default;\n} catch {\n\tuWS = null;\n}\nconst describeUWS = uWS ? describe : describe.skip;\n\nlet server;\n\ndescribeUWS('reserved",
				"ours": "// The reserved `/__realtime/*` admin route, driven over the node:http\n// server createTestServer builds.\n\nlet server;\n\ndescribe('reserved"
			},
			{
				"lead": "\t\t// With no admin route mounted, the request falls through to uWS's\n\t\t// built-in 404. A mounted route would return our JSON (200/500), never\n\t\t// a uWS \"File Not Found\" 404 - so this proves the route is absent.",
				"ours": "\t\t// With no admin route mounted, the request falls through to the\n\t\t// harness's built-in 404. A mounted route would return our JSON\n\t\t// (200/500), never a plain 404 - so this proves the route is absent."
			},
			{
				"lead": "\t\t// ...and NOT at the default prefix (uWS built-in 404, route absent there).",
				"ours": "\t\t// ...and NOT at the default prefix (harness 404, route absent there)."
			},
			{
				"lead": "\ndescribeUWS('platform.introspect transport snapshot', () => {\n\tafterEach(async () => {\n\t\tawait server?.close();\n\t\tserver = null;\n\t});\n\n\tit('returns a PII-free scalar snapshot (no topic names)', async () => {\n\t\tconst { createTestServer } = await import('../src/testing.js');\n\t\tserver = await createTestServer();\n\t\tconst snap = server.platform.introspect();\n\n\t\texpect(typeof snap.connections).toBe('number');\n\t\texpect(typeof snap.closedWsAborts).toBe('number');\n\t\texpect(typeof snap.maxPayloadLength).toBe('number');\n\t\texpect(['normal', 'elevated', 'siege']).toContain(snap.protection);\n\n\t\t// Scalar pressure signals only - topPublishers (topic names) is omitted.\n\t\texpect(snap.pressure).toBeTypeOf('object');\n\t\texpect('topPublishers' in snap.pressure).toBe(false);\n\t\texpect(typeof snap.pressure.reason).toBe('string');\n\t\texpect(typeof snap.pressure.active).toBe('boolean');\n\n\t\t// assertions is a plain object snapshot (not the live Map).\n\t\texpect(snap.assertions).toBeTypeOf('object');\n\t\texpect(snap.assertions instanceof Map).toBe(false);\n\n\t\t// Default introspection exposes only bounded diagnostic metadata. Detailed\n\t\t// keyed stream evidence requires an exact opaque id through the admin gate.\n\t\texpect(snap.diagnostics).toEqual({ retained: 0, recent: [] });\n\t\texpect(server.platform.diagnostic('unknown')).toBeNull();\n\t});\n\n\tit('reflects the live connection count', async () => {\n\t\tconst { WebSocket } = await import('ws');\n\t\tconst { createTestServer } = await import('../src/testing.js');\n\t\tserver = await createTestServer();\n\t\texpect(server.platform.introspect().connections).toBe(0);\n\n\t\tconst ws = new WebSocket(server.wsUrl);\n\t\tawait new Promise((resolve, reject) => {\n\t\t\tws.on('open', resolve);\n\t\t\tws.on('error', reject);\n\t\t});\n\t\t// The server-side open handler can lag the client open event slightly;\n\t\t// poll briefly until the connection registers.\n\t\tfor (let i = 0; i < 50 && server.platform.introspect().connections === 0; i++) {\n\t\t\tawait new Promise((r) => setTimeout(r, 10));\n\t\t}\n\t\texpect(server.platform.introspect().connections).toBe(1);\n\t\tws.close();\n\t});\n\n\tit('composes through a realtime-style admin handler under a transport key', async () => {\n\t\tconst { createTestServer } = await import('../src/testing.js');\n\t\t// Model svelte-realtime's introspect() merging platform.introspect()\n\t\t// under a `transport` key, exposed via the admin route.\n\t\tserver = await createTestServer({\n\t\t\thandler: {\n\t\t\t\tadmin: async (request) => {\n\t\t\t\t\tconst sub = new URL(request.url).pathname.replace('/__realtime/', '');\n\t\t\t\t\tif (sub !== 'introspect') return new Response('not found', { status: 404 });\n\t\t\t\t\tconst transport = server.platform.introspect();\n\t\t\t\t\treturn Response.json({ handlers: { total: 0 }, transport });\n\t\t\t\t}\n\t\t\t}\n\t\t});\n\t\tconst res = await fetch(`${server.url}/__realtime/introspect`);\n\t\texpect(res.status).toBe(200);\n\t\tconst body = await res.json();\n\t\texpect(body.transport).toBeTypeOf('object');\n\t\texpect(typeof body.transport.connections).toBe('number');\n\t\texpect(body.handlers.total).toBe(0);\n\t});\n\n\t// The route matches the RAW target and building a Request normalizes dot\n\t// segments, so `/__realtime/../reflect` matched here and handed `admin()` a\n\t// request calling itself `/reflect` - outside the prefix that was routed and\n\t// outside the namespace the handler dispatches and authorizes on.\n\t//\n\t// RAW SOCKET, not fetch(): every HTTP client normalizes the target before it\n\t// goes out, so `fetch('/__realtime/../reflect')` puts `/reflect` on the wire\n\t// and never reaches this route. A case written with fetch passes against the\n\t// defect while testing nothing.\n\tit('refuses a target that normalizes out of the admin prefix', async () => {\n\t\t/** @type {string[]} */\n\t\tconst seen = [];\n\t\tconst { createTestServer } = await import('../src/testing.js');\n\t\tserver = await createTestServer({\n\t\t\thandler: {\n\t\t\t\tadmin: async (request) => {\n\t\t\t\t\tseen.push(new URL(request.url).pathname);\n\t\t\t\t\treturn new Response('reached', { status: 200 });\n\t\t\t\t}\n\t\t\t}\n\t\t});\n\t\tconst port = Number(new URL(server.url).port);\n\t\tconst net = await import('node:net');\n\n\t\t/** @param {string} target */\n\t\tconst raw = (target) => new Promise((resolve) => {\n\t\t\tlet buf = '';\n\t\t\tconst sock = net.connect(port, '127.0.0.1', () => {\n\t\t\t\tsock.write(`GET ${target} HTTP/1.1\\r\\nHost: 127.0.0.1:${port}\\r\\nConnection: close\\r\\n\\r\\n`);\n\t\t\t});\n\t\t\tsock.setTimeout(5000, () => { sock.destroy(); resolve(0); });\n\t\t\tsock.on('data', (d) => { buf += d.toString(); });\n\t\t\t// The status LINE, then the code: uWS writes no reason phrase, so\n\t\t\t// splitting the whole buffer on spaces takes a header with it.\n\t\t\tsock.on('close', () => resolve(Number(buf.split('\\r\\n')[0].split(' ')[1] || 0)));\n\t\t\tsock.on('error', () => resolve(0));\n\t\t});\n\n\t\t// Vacuity guard: this transport reaches the handler when the target is\n\t\t// ordinary, so the refusals below are the check and not a dead route.\n\t\texpect(await raw('/__realtime/introspect')).toBe(200);\n\t\texpect(seen).toEqual(['/__realtime/introspect']);\n\n\t\tseen.length = 0;\n\t\t// `%2e` is a dot segment to the URL parser, so the encoded spelling\n\t\t// normalizes identically and must be refused identically.\n\t\tfor (const target of ['/__realtime/../reflect', '/__realtime/a/../../escape', '/__realtime/%2e%2e/reflect']) {\n\t\t\texpect(await raw(target), `${target} must be refused`).toBe(400);\n\t\t}\n\t\texpect(seen, 'a target outside the prefix reached the app handler').toEqual([]);\n\n\t\t// Normalization is not the fault - landing outside the prefix is. A\n\t\t// target that resolves back INSIDE is still served, and the handler is\n\t\t// told the resolved path, which is what it should dispatch on.\n\t\texpect(await raw('/__realtime/a/../introspect')).toBe(200);\n\t\texpect(seen).toEqual(['/__realtime/introspect']);\n\t});\n});\n",
				"ours": ""
			}
		]
	},
	{
		"file": "test/admission-metrics.test.js",
		"adaptations": [
			{
				"lead": "// uWS server (createTestServer harness) with a recording registry passed via\n",
				"ours": "// server (the createTestServer harness) with a recording registry passed via\n"
			},
			{
				"lead": "let uWS;\ntry {\n\tuWS = (await import('uWebSockets.js')).default;\n} catch {\n\tuWS = null;\n}\n\nconst describeUWS = uWS ? describe : describe.skip;\n",
				"ours": ""
			},
			{
				"lead": "describeUWS('admission metrics on createTestServer', () => {\n",
				"ours": "describe('admission metrics on createTestServer', () => {\n"
			},
			{
				"lead": "const CURSOR_SUBPROTOCOL = 'svelte-realtime-cursor';\n\n",
				"ours": "const CURSOR_SUBPROTOCOL = 'svelte-realtime-cursor';\n\n// The client's own `close` event is not proof the server has finished with the\n// connection: on this transport the peer's FIN is acknowledged to the client\n// before the server's close handler has run, so the permit is handed back a\n// turn later. Poll the gauge instead of sampling it once.\nasync function waitForGauge(metrics, name, value, timeoutMs = 2000) {\n\tconst deadline = Date.now() + timeoutMs;\n\twhile (metrics.gaugeValue(name) !== value && Date.now() < deadline) {\n\t\tawait new Promise((r) => setTimeout(r, 5));\n\t}\n\treturn metrics.gaugeValue(name);\n}\n\n"
			},
			{
				"lead": "\t\t\tfirst.ws.once('close', resolve);\n\t\t\tfirst.ws.close();\n\t\t});\n\t\texpect(metrics.gaugeValue('ws_connection_headroom')).toBe(1);",
				"ours": "\t\t\tfirst.ws.once('close', resolve);\n\t\t\tfirst.ws.close();\n\t\t});\n\t\texpect(await waitForGauge(metrics, 'ws_connection_headroom', 1)).toBe(1);"
			},
			{
				"lead": "\t\t\tconnection.ws.once('close', resolve);\n\t\t\tconnection.ws.close();\n\t\t});\n\t\texpect(metrics.gaugeValue('ws_connection_headroom')).toBe(1);",
				"ours": "\t\t\tconnection.ws.once('close', resolve);\n\t\t\tconnection.ws.close();\n\t\t});\n\t\texpect(await waitForGauge(metrics, 'ws_connection_headroom', 1)).toBe(1);"
			},
			{
				"lead": "\t// the refusal runs synchronously, inside the uWS route handler, where the\n\t// request is still valid. Every shipped test was on that side of the line.\n\t//\n\t// With a hook that resolves in a LATER TICK - which is every hook doing real\n\t// I/O, and so the ordinary auth-carrying shape - the refusal runs from the\n\t// hook's `.then()`. uWS invalidates the stack-allocated request at the END OF\n\t// THE NATIVE TICK (not at route-handler return: a hook awaiting only settled\n\t// promises stays inside the tick and was always fine), so the refusal's header\n\t// reads threw, the catch around the hook swallowed it, and the client got a\n\t// 500 it cannot tell from a broken server - for a NORMAL, expected shed.\n\t//\n\t// It also double-counted: the same attempt landed on `deferred_overflow` and\n\t// then again on `hook_error`, breaking the accounting invariant the\n\t// over-capacity test above pins.\n",
				"ours": "\t// the refusal runs synchronously inside the route handler.\n\t//\n\t// With a hook that resolves in a LATER TICK - which is every hook doing real\n\t// I/O, and so the ordinary auth-carrying shape - the refusal runs from the\n\t// hook's `.then()`. The contract is the same either way: a shed answers 503\n\t// and is counted once as `deferred_overflow`, never a 500 and never counted\n\t// again as `hook_error`. A 500 is indistinguishable from a broken server,\n\t// and the second count breaks the accounting invariant the over-capacity\n\t// test above pins.\n\t//\n\t// A transport whose request object dies with the native tick that produced\n\t// it reaches that 500 by throwing on the late header reads. This one hands\n\t// the upgrade a plain header bag that outlives the tick, so the throw\n\t// cannot arise here and the case pins the answer rather than the mechanism.\n"
			}
		]
	},
	{
		"file": "test/assertion-metrics.test.js"
	},
	{
		"file": "test/auditor-safety.test.js"
	},
	{
		"file": "test/auditor-snapshot.test.js"
	},
	{
		"file": "test/auditor.test.js"
	},
	{
		"file": "test/channels.test.js"
	},
	{
		"file": "test/chaos.test.js",
		"adaptations": [
			{
				"lead": "let uWS;\ntry {\n\tuWS = (await import('uWebSockets.js')).default;\n} catch {\n\tuWS = null;\n}\n",
				"ours": ""
			},
			{
				"lead": "const describeUWS = uWS ? describe : describe.skip;\n",
				"ours": ""
			},
			{
				"lead": "describeUWS('chaos / fault-injection harness on createTestServer', () => {\n",
				"ours": "describe('chaos / fault-injection harness on createTestServer', () => {\n"
			}
		]
	},
	{
		"file": "test/client-binary.test.js"
	},
	{
		"file": "test/client-import-graph.test.js"
	},
	{
		"file": "test/client-offline.test.js"
	},
	{
		"file": "test/client-reconnect-advisory.test.js"
	},
	{
		"file": "test/client-suspend.test.js"
	},
	{
		"file": "test/client.test.js"
	},
	{
		"file": "test/cluster-relay-attach-gate.test.js"
	},
	{
		"file": "test/codec-relay.test.js",
		"adaptations": [
			{
				"lead": "let uWS;\ntry {\n\tuWS = (await import('uWebSockets.js')).default;\n} catch {\n\tuWS = null;\n}\nconst describeUWS = uWS ? describe : describe.skip;\nconst { createTestServer } = uWS ? await import('../src/testing.js') : {};\n",
				"ours": "const { createTestServer } = await import('../src/testing.js');\n"
			},
			{
				"lead": " * A scripted fake connection shaped like the subset of the uWS socket the publish\n",
				"ours": " * A scripted fake connection shaped like the subset of the harness socket the publish\n"
			},
			{
				"lead": "describeUWS('codec-aware cross-worker relay re-encode (relayPublishWire)', () => {\n",
				"ours": "describe('codec-aware cross-worker relay re-encode (relayPublishWire)', () => {\n"
			}
		]
	},
	{
		"file": "test/connection-permit-carrier.test.js"
	},
	{
		"file": "test/crdt-channel.test.js"
	},
	{
		"file": "test/crdt-codec.test.js"
	},
	{
		"file": "test/crdt-replica.test.js"
	},
	{
		"file": "test/cursor-decode.test.js"
	},
	{
		"file": "test/cursor-handle.test.js"
	},
	{
		"file": "test/cursor-presence-tap-wire.test.js",
		"adaptations": [
			{
				"lead": "// Unlike the mock-ws unit suites, these run a REAL uWebSockets.js server\n",
				"ours": "// Unlike the mock-ws unit suites, these run a REAL node:http + ws server\n"
			},
			{
				"lead": "let uWS;\ntry {\n\tuWS = (await import('uWebSockets.js')).default;\n} catch {\n\tuWS = null;\n}\nconst describeUWS = uWS ? describe : describe.skip;\n",
				"ours": ""
			},
			{
				"lead": "describeUWS('cursor + presence tap channels over a real uWS server', () => {\n",
				"ours": "describe('cursor + presence tap channels over a real server', () => {\n"
			}
		]
	},
	{
		"file": "test/cursor-render.test.js"
	},
	{
		"file": "test/cursor-viewport-client.test.js"
	},
	{
		"file": "test/cursor-worker-wire.test.js",
		"adaptations": [
			{
				"lead": "// The cursor render worker against a REAL uWS server (createTestServer) and\n",
				"ours": "// The cursor render worker against a REAL server (createTestServer) and\n"
			},
			{
				"lead": "let uWS;\ntry {\n\tuWS = (await import('uWebSockets.js')).default;\n} catch {\n\tuWS = null;\n}\nconst describeUWS = uWS ? describe : describe.skip;\nconst { createTestServer } = uWS ? await import('../src/testing.js') : {};\n",
				"ours": "const { createTestServer } = await import('../src/testing.js');\n"
			},
			{
				"lead": "describeUWS('cursor render worker against a real server', () => {\n",
				"ours": "describe('cursor render worker against a real server', () => {\n"
			}
		]
	},
	{
		"file": "test/cursor-worker.test.js"
	},
	{
		"file": "test/cursor-you.test.js"
	},
	{
		"file": "test/cursor.test.js"
	},
	{
		"file": "test/dedup.test.js"
	},
	{
		"file": "test/dev-batch-landing-order.test.js"
	},
	{
		"file": "test/dev-batch-options.test.js"
	},
	{
		"file": "test/dev-dashboard.test.js"
	},
	{
		"file": "test/divergence-diagnostics.test.js"
	},
	{
		"file": "test/dst-goldens/adapter-cluster.golden.json"
	},
	{
		"file": "test/dst-goldens/adapter-single.golden.json"
	},
	{
		"file": "test/egress-account.test.js"
	},
	{
		"file": "test/egress-ceilings.test.js"
	},
	{
		"file": "test/fixture/package.json"
	},
	{
		"file": "test/fixture/src/app.html"
	},
	{
		"file": "test/fixture/src/handshake-vectors.js"
	},
	{
		"file": "test/fixture/src/hooks.ws.attribution.js"
	},
	{
		"file": "test/fixture/src/hooks.ws.batchleak.js"
	},
	{
		"file": "test/fixture/src/hooks.ws.crlf.js"
	},
	{
		"file": "test/fixture/src/hooks.ws.egress.js"
	},
	{
		"file": "test/fixture/src/hooks.ws.grant.js"
	},
	{
		"file": "test/fixture/src/hooks.ws.hookcrash.js"
	},
	{
		"file": "test/fixture/src/hooks.ws.js"
	},
	{
		"file": "test/fixture/src/hooks.ws.park.js"
	},
	{
		"file": "test/fixture/src/hooks.ws.parkjoin.js"
	},
	{
		"file": "test/fixture/src/hooks.ws.parkmany.js"
	},
	{
		"file": "test/fixture/src/hooks.ws.presence.js"
	},
	{
		"file": "test/fixture/src/hooks.ws.recover.js"
	},
	{
		"file": "test/fixture/src/hooks.ws.respawner.js"
	},
	{
		"file": "test/fixture/src/hooks.ws.resumespill.js"
	},
	{
		"file": "test/fixture/src/hooks.ws.strict-grant.js"
	},
	{
		"file": "test/fixture/src/hooks.ws.subbatch.js"
	},
	{
		"file": "test/fixture/src/hooks.ws.subhook.js"
	},
	{
		"file": "test/fixture/src/hooks.ws.workercrash.js"
	},
	{
		"file": "test/fixture/src/metrics.js"
	},
	{
		"file": "test/fixture/src/primary-init.js"
	},
	{
		"file": "test/fixture/src/routes/+page.server.js"
	},
	{
		"file": "test/fixture/src/routes/+page.svelte"
	},
	{
		"file": "test/fixture/src/routes/__leak/+server.js"
	},
	{
		"file": "test/fixture/src/routes/cursors/+page.svelte"
	},
	{
		"file": "test/fixture/src/routes/metrics-direct/+server.js"
	},
	{
		"file": "test/fixture/src/routes/metrics/+server.js"
	},
	{
		"file": "test/fixture/src/routes/slow-hold/+server.js"
	},
	{
		"file": "test/fixture/src/routes/smooth/+page.svelte"
	},
	{
		"file": "test/fixture/src/routes/ssr-echo/+server.js"
	},
	{
		"file": "test/fixture/src/routes/ssr-first-chunk-fail/+server.js"
	},
	{
		"file": "test/fixture/src/routes/ssr-stream-fail/+server.js"
	},
	{
		"file": "test/fixture/src/tracing.invalid.js"
	},
	{
		"file": "test/fixture/src/waiting-room.renderer.js"
	},
	{
		"file": "test/fixture/static/.htpasswd"
	},
	{
		"file": "test/fixture/static/.well-known/.nested-secret"
	},
	{
		"file": "test/fixture/static/.well-known/probe.txt"
	},
	{
		"file": "test/fixture/static/deep/.hidden.txt"
	},
	{
		"file": "test/fixture/static/test.txt"
	},
	{
		"file": "test/fixture/svelte.config.js"
	},
	{
		"file": "test/fixture/variants.js"
	},
	{
		"file": "test/fixture/vite.config.js"
	},
	{
		"file": "test/groups-wire-policy.test.js",
		"adaptations": [
			{
				"lead": "let uWS;\ntry {\n\tuWS = (await import('uWebSockets.js')).default;\n} catch {\n\tuWS = null;\n}\nconst describeUWS = uWS ? describe : describe.skip;\n",
				"ours": ""
			},
			{
				"lead": "describeUWS('groups wire authorization policy', () => {\n",
				"ours": "describe('groups wire authorization policy', () => {\n"
			}
		]
	},
	{
		"file": "test/groups.test.js"
	},
	{
		"file": "test/helpers/fixture-build.js"
	},
	{
		"file": "test/helpers/global-setup.js"
	},
	{
		"file": "test/helpers/handshake-vectors.js"
	},
	{
		"file": "test/helpers/live-runtimes.js"
	},
	{
		"file": "test/helpers/metrics-snapshot-worker.mjs"
	},
	{
		"file": "test/helpers/real-runtime.js",
		"adaptations": [
			{
				"lead": "let uWS;\ntry {\n\tuWS = createRequire(import.meta.url)('uWebSockets.js');\n} catch {\n\tuWS = null;\n}",
				"ours": "let transport;\ntry {\n\ttransport = createRequire(import.meta.url)('ws');\n} catch {\n\ttransport = null;\n}"
			},
			{
				"lead": " * uWS present? Suites gate with `hasUWS ? describe : describe.skip`.\n *\n * uWebSockets.js is an OPTIONAL native dependency, so a contributor without it\n * can still run the pure suites - that is why the gate exists and it stays.\n * What is not acceptable is the shape of the result: a skipped real-runtime\n * suite reports PASSED with zero assertions, which is indistinguishable from a\n * suite that ran and proved something. Every claim these suites make about\n * authorization, revocation and the handshake would be reported as verified by\n * a run that verified nothing.\n *\n * So where the result is a GATE rather than a convenience, absence is a hard\n * failure instead: `REQUIRE_UWS=1` demands it explicitly, and `CI` implies it.\n */\nexport const hasUWS = uWS !== null;",
				"ours": " * Transport present? Suites gate with `hasUWS ? describe : describe.skip`.\n *\n * The name is the lead's and stays: the suites that read it are carried\n * verbatim from there, and what it means on both sides is \"the transport this\n * runtime needs is installed\". What differs is the answer. There the transport\n * is an optional NATIVE dependency a contributor may not have, so the gate is a\n * real convenience; here it is `ws`, an ordinary dependency of this package,\n * so the answer is always yes and every real-runtime suite RUNS.\n *\n * That matters more than it looks. A skipped suite reports PASSED with zero\n * assertions, which is indistinguishable from one that ran and proved\n * something - so a gate that could never be false here would quietly report\n * every claim about authorization, revocation and the handshake as verified by\n * a run that verified nothing.\n */\nexport const hasUWS = transport !== null;"
			},
			{
				"lead": "\t\t'uWebSockets.js is not installed, so every real-runtime suite would skip and report ' +",
				"ours": "\t\t'ws is not installed, so every real-runtime suite would skip and report ' +"
			}
		]
	},
	{
		"file": "test/helpers/restore-globals.js"
	},
	{
		"file": "test/helpers/stop-leaked-runtimes.js"
	},
	{
		"file": "test/invariants.test.js"
	},
	{
		"file": "test/leak-auditor.test.js"
	},
	{
		"file": "test/leak-detect.test.js"
	},
	{
		"file": "test/lock.test.js"
	},
	{
		"file": "test/metrics-snapshot.test.js"
	},
	{
		"file": "test/middleware.test.js"
	},
	{
		"file": "test/observability-privacy.test.js"
	},
	{
		"file": "test/os-posture.test.js"
	},
	{
		"file": "test/plugin-cluster-authority.test.js",
		"adaptations": [
			{
				"lead": "// in test/cluster-sequence-policy-real.test.js (group-roundtrip and",
				"ours": "// in the lead adapter's test/cluster-sequence-policy-real.test.js (group-roundtrip and"
			}
		]
	},
	{
		"file": "test/posture-collector.test.js"
	},
	{
		"file": "test/presence-client.test.js"
	},
	{
		"file": "test/presence-codec.test.js"
	},
	{
		"file": "test/presence-heartbeat-recovery.test.js"
	},
	{
		"file": "test/presence.test.js"
	},
	{
		"file": "test/protection-posture-unit.test.js",
		"adaptations": [
			{
				"lead": "// Pure-function coverage for the graduated protection posture and the capacity\n// reason override. These tests drive the exported helpers directly and import\n// nothing that pulls in uWebSockets.js, so they evaluate identically under every\n// pool worker and cannot flake on cross-file module-load ordering. The live\n// createTestServer coupling lives in protection-posture.test.js.",
				"ours": "// Pure-function coverage for the graduated protection posture and the capacity\n// reason override. These tests drive the exported helpers directly, so they\n// evaluate identically under every pool worker and cannot flake on cross-file\n// module-load ordering. The live createTestServer coupling lives in\n// protection-posture.test.js."
			},
			{
				"lead": "import {\n\tcreatePosture,\n\tapplyCapacityReason,\n\tcreateUpgradeAdmission,\n\tcomputePressureReason\n} from '../src/runtime/utils.js';",
				"ours": "import { createPosture, applyCapacityReason, computePressureReason } from '../src/runtime/utils/pressure.js';\nimport { createUpgradeAdmission } from '../src/runtime/utils/upgrade-admission.js';"
			}
		]
	},
	{
		"file": "test/protection-posture.test.js",
		"adaptations": [
			{
				"lead": "// The pure createPosture / applyCapacityReason assertions live in\n// protection-posture-unit.test.js (no uWebSockets.js import) so they evaluate\n// deterministically under every pool worker. This file holds only the live\n// integration, and runs its server-backed describe blocks sequentially so two\n// uWS instances never contend on port/socket churn within the file.\n\nimport { describe, it, expect, afterEach } from 'vitest';\n\nlet uWS;\ntry {\n\tuWS = (await import('uWebSockets.js')).default;\n} catch {\n\tuWS = null;\n}\n\nconst describeUWS = uWS ? describe.sequential : describe.skip;\n\nlet server;",
				"ours": "// The pure createPosture / applyCapacityReason assertions live in\n// protection-posture-unit.test.js so they evaluate deterministically under\n// every pool worker. This file holds only the live integration, and runs its\n// server-backed describe blocks sequentially so two servers never contend on\n// port churn within the file.\n\nimport { describe, it, expect, afterEach } from 'vitest';\n\nlet server;"
			},
			{
				"lead": "describeUWS('protection posture coupling on createTestServer', () => {",
				"ours": "describe.sequential('protection posture coupling on createTestServer', () => {"
			}
		]
	},
	{
		"file": "test/queue.test.js"
	},
	{
		"file": "test/ratelimit.test.js"
	},
	{
		"file": "test/relay-gap.test.js",
		"adaptations": [
			{
				"lead": "const src = read('../src/runtime/handler/lifecycle.js');",
				"ours": "const src = read('../src/runtime/handler/platform.js');"
			},
			{
				"lead": "const src = read('../src/runtime/handler.js');",
				"ours": "const src = read('../src/runtime/handler/realtime.js');"
			}
		]
	},
	{
		"file": "test/relay-marker-authority.test.js",
		"adaptations": [
			{
				"lead": "adapter-uws.relay-",
				"ours": "adapter-ws.relay-",
				"all": true
			}
		]
	},
	{
		"file": "test/replay.test.js"
	},
	{
		"file": "test/safe-url.test.js"
	},
	{
		"file": "test/send-seq.test.js",
		"adaptations": [
			{
				"lead": "let uWS = null;\ntry {\n\tuWS = (await import('uWebSockets.js')).default;\n} catch {\n\tuWS = null;\n}\n// Visible skip, never a silent in-body return: a case that returns early on a\n// missing binding reports PASSED with zero assertions, the reporting shape\n// helpers/real-runtime.js exists to forbid.\nconst itUWS = uWS ? it : it.skip;",
				"ours": "// The lead gates this case on a native binding a contributor may not have.\n// Here the transport is `ws`, an ordinary dependency of this package, so the\n// answer is always yes and the case RUNS - the same reasoning\n// helpers/real-runtime.js records for its own always-true `hasUWS`.\nconst itUWS = it;"
			},
			{
				"lead": "\t/** @type {any} */ let symbols;",
				"ours": "\t/** @type {any} */ let symbols;\n\t/** @type {any} */ let resume;"
			},
			{
				"lead": "\t\tsymbols = await import(pathToFileURL(path.join(builtDir, '..', 'utils.js')).href);",
				"ours": "\t\tsymbols = await import(pathToFileURL(path.join(builtDir, '..', 'utils.js')).href);\n\t\tresume = await import(pathToFileURL(path.join(builtDir, 'resume-capture.js')).href);"
			},
			{
				"lead": "\t\tconst capture = { frames: [], overflow: false };\n\t\tstate.resumeBuffers.set(topic, new Set([capture]));",
				"ours": "\t\t// Opened through the production opener rather than by seeding a module\n\t\t// map: this repo keeps the resume buffers in handler/resume-capture.js,\n\t\t// and driving the real registration path is what a stray capture would\n\t\t// actually have to survive.\n\t\tconst handle = resume.beginResumeCapture([topic], ws);\n\t\tconst capture = handle.entries[0].buffer;"
			},
			{
				"lead": "\t\t\tstate.resumeBuffers.delete(topic);",
				"ours": "\t\t\tresume.discardResumeCapture(handle);"
			}
		]
	},
	{
		"file": "test/seq-stamp.test.js"
	},
	{
		"file": "test/session.test.js"
	},
	{
		"file": "test/sim-core.test.js"
	},
	{
		"file": "test/sim-git-commit.test.js"
	},
	{
		"file": "test/sim-golden.test.js"
	},
	{
		"file": "test/sim-leak.test.js"
	},
	{
		"file": "test/sim-multiworker.test.js"
	},
	{
		"file": "test/sim-swarm.test.js"
	},
	{
		"file": "test/sim.test.js"
	},
	{
		"file": "test/smooth-authority.test.js"
	},
	{
		"file": "test/smooth-cell-codec.test.js",
		"adaptations": [
			{
				"lead": "let uWS;\ntry {\n\tuWS = (await import('uWebSockets.js')).default;\n} catch {\n\tuWS = null;\n}\nconst describeUWS = uWS ? describe : describe.skip;\nconst { createTestServer } = uWS ? await import('../src/testing.js') : {};\n",
				"ours": "const { createTestServer } = await import('../src/testing.js');\n"
			},
			{
				"lead": "describeUWS('cell codec native cohort fan-out', () => {\n",
				"ours": "describe('cell codec native cohort fan-out', () => {\n"
			}
		]
	},
	{
		"file": "test/smooth-channel.test.js"
	},
	{
		"file": "test/smooth-clock.test.js"
	},
	{
		"file": "test/smooth-codec.test.js"
	},
	{
		"file": "test/smooth-cursor-worker.test.js"
	},
	{
		"file": "test/smooth-events.test.js"
	},
	{
		"file": "test/smooth-interpolate.test.js"
	},
	{
		"file": "test/smooth-local-motion.test.js"
	},
	{
		"file": "test/smooth-noecho.test.js",
		"adaptations": [
			{
				"lead": "// Sender exclusion on publishWire against a REAL uWS server (createTestServer,\n",
				"ours": "// Sender exclusion on publishWire against a REAL server (createTestServer,\n"
			},
			{
				"lead": "let uWS;\ntry {\n\tuWS = (await import('uWebSockets.js')).default;\n} catch {\n\tuWS = null;\n}\nconst describeUWS = uWS ? describe : describe.skip;\nconst { createTestServer } = uWS ? await import('../src/testing.js') : {};\n",
				"ours": "const { createTestServer } = await import('../src/testing.js');\n"
			},
			{
				"lead": "describeUWS('publishWire sender exclusion against a real server', () => {\n",
				"ours": "describe('publishWire sender exclusion against a real server', () => {\n"
			}
		]
	},
	{
		"file": "test/smooth-predict.test.js",
		"adaptations": [
			{
				"lead": "import { createSeededRng } from '../src/sim.js';",
				"ours": "import { createSeededRng } from '../src/runtime/sim-core.js';"
			}
		]
	},
	{
		"file": "test/smooth-random.test.js"
	},
	{
		"file": "test/smooth-wire-batch.test.js",
		"adaptations": [
			{
				"lead": "// Platform fan-out (real server; skipped when uWebSockets.js is unavailable).\n",
				"ours": "// Platform fan-out (real server).\n"
			},
			{
				"lead": "let uWS;\ntry {\n\tuWS = (await import('uWebSockets.js')).default;\n} catch {\n\tuWS = null;\n}\nconst describeUWS = uWS ? describe : describe.skip;\nconst { createTestServer } = uWS ? await import('../src/testing.js') : {};\n",
				"ours": "const { createTestServer } = await import('../src/testing.js');\n"
			},
			{
				"lead": "describeUWS('publishWireBatch fan-out', () => {\n",
				"ours": "describe('publishWireBatch fan-out', () => {\n"
			},
			{
				"lead": "describeUWS('sendWireBatch (per-subscriber culled delivery)', () => {\n",
				"ours": "describe('sendWireBatch (per-subscriber culled delivery)', () => {\n"
			}
		]
	},
	{
		"file": "test/smooth-wire-view.test.js"
	},
	{
		"file": "test/smooth-wire.test.js"
	},
	{
		"file": "test/state-convergence.test.js"
	},
	{
		"file": "test/state-hash-detector.test.js"
	},
	{
		"file": "test/steadystate.test.js"
	},
	{
		"file": "test/throttle.test.js"
	},
	{
		"file": "test/transport-metrics.test.js"
	},
	{
		"file": "test/version-info.test.js",
		"adaptations": [
			{
				"lead": "svelte-adapter-ws-extensions",
				"ours": "svelte-adapter-uws-extensions",
				"all": true
			}
		]
	},
	{
		"file": "test/vite-grant-conjunct.test.js"
	},
	{
		"file": "test/vite-max-payload.test.js"
	},
	{
		"file": "test/vite-origin-parity.test.js"
	},
	{
		"file": "test/vite.test.js",
		"adaptations": [
			{
				"lead": "'adapter-uws'",
				"ours": "'adapter-ws'",
				"all": true
			}
		]
	},
	{
		"file": "test/warmup.test.js"
	},
	{
		"file": "test/webhooks-controls.test.js"
	},
	{
		"file": "test/webhooks-delivery.test.js"
	},
	{
		"file": "test/webhooks-destination.test.js"
	},
	{
		"file": "test/webhooks.test.js"
	},
	{
		"file": "test/websocket-option-contract.test.js"
	},
	{
		"file": "test/ws-handler-resolution.test.js",
		"adaptations": [
			{
				"lead": "// the app has, and an app-supplied `subscribe` hook stands the server-grant\n// model down (see src/runtime/handler/subscribe-hooks.js), so a substitution\n// can disarm a gate the operator explicitly armed.\n",
				"ours": "// the app has, and an app-supplied `subscribe` hook stands the server-grant\n// model down (see the subscribe authorization in\n// src/runtime/handler/realtime.js), so a substitution can disarm a gate the\n// operator explicitly armed.\n"
			},
			{
				"lead": "// The artifact test below therefore asserts on the BUILT BUNDLE, not on the\n// adapter's normalized options: the option object was always correct: it was\n// simply never read, so an options-level assertion stays green against the bug.\n",
				"ours": "// An options-level assertion stays green against that bug on its own, because\n// the option object was always correct - it was simply never read. So what\n// these cases pin is the adapter PUBLISHING the option to the plugin, and the\n// guard that refuses a build whose bundle names a different module.\n"
			},
			{
				"lead": "import { readFileSync } from 'node:fs';\n",
				"ours": ""
			},
			{
				"lead": "import { buildFixtureOnce } from './helpers/fixture-build.js';\nimport { hasUWS, REAL_BOOT_BUDGET_MS } from './helpers/real-runtime.js';\nimport { variantOut, FIXTURE_VARIANTS } from './fixture/variants.js';\n",
				"ours": ""
			},
			{
				"lead": "const fixtureDir = path.join(ROOT, 'test/fixture');\n",
				"ours": ""
			},
			{
				"lead": "\tit('publishes the configured module path on the adapter object', () => {\n\t\texpect(adapter({ websocket: { metrics: './src/m.js' } }).websocketMetrics).toBe('./src/m.js');\n\t});\n\n",
				"ours": ""
			},
			{
				"lead": "// The real proof. Everything above tests the guard; this tests that the option\n// actually reaches the build, which is what was broken.\nconst describeBuild = hasUWS ? describe : describe.skip;\n\ndescribeBuild('the built artifact honours websocket.handler through the Vite plugin', () => {\n\tit('bundles the module named on the adapter, not the auto-discovered one', () => {\n\t\t// The `grant` variant names its handler ONLY through the adapter's\n\t\t// `websocket.handler` (test/fixture/svelte.config.js); the fixture's\n\t\t// vite.config.js passes the plugin nothing. So if the plugin stops\n\t\t// honouring the adapter's option, auto-discovery picks up\n\t\t// src/hooks.ws.js and every assertion below flips.\n\t\texpect(FIXTURE_VARIANTS.grant.handler).toBe('./src/hooks.ws.grant.js');\n\n\t\tconst built = buildFixtureOnce('grant');\n\t\texpect(built, 'grant fixture variant failed to build').toBe(true);\n\n\t\tconst bundle = readFileSync(\n\t\t\tpath.join(fixtureDir, variantOut('grant'), 'server/ws-handler.js'),\n\t\t\t'utf8'\n\t\t);\n\n\t\t// A marker unique to the named module...\n\t\texpect(bundle).toContain('resume-topics');\n\t\t// ...and one unique to the module auto-discovery would have picked.\n\t\texpect(bundle).not.toContain('cork-test');\n\t\t// Reads a build rather than performing one on a warm tree, but the call\n\t\t// is the same one either way and a cold or contended tree pays for the\n\t\t// build here - which is more than vitest's default allows for.\n\t}, REAL_BOOT_BUDGET_MS);\n\n\tit('carries the named handler plugin hook, not the auto-discovered authorization hook', () => {\n\t\t// The named handler now wraps a real groups-plugin side-effect hook and\n\t\t// preserves its marker. Its export is expected and does not disarm the\n\t\t// grant model; the differential suite proves that behavior. These bundle\n\t\t// markers distinguish it from src/hooks.ws.js, whose app authorization\n\t\t// hook would stand the armed gate down.\n\t\texpect(buildFixtureOnce('grant')).toBe(true);\n\n\t\tconst bundle = readFileSync(\n\t\t\tpath.join(fixtureDir, variantOut('grant'), 'server/ws-handler.js'),\n\t\t\t'utf8'\n\t\t);\n\t\tconst exportList = /\\bexport\\s*\\{([^}]*)\\}/.exec(bundle);\n\t\texpect(exportList, 'bundled handler has no export statement to inspect').not.toBeNull();\n\n\t\tconst names = exportList[1]\n\t\t\t.split(',')\n\t\t\t.map((entry) => entry.trim().split(/\\s+as\\s+/).pop().trim())\n\t\t\t.filter(Boolean);\n\n\t\texpect(names).toContain('upgrade');\n\t\texpect(names).toContain('subscribe');\n\t\texpect(bundle).toContain('policy-lobby');\n\t\texpect(bundle).toContain('hook-entered');\n\t}, REAL_BOOT_BUDGET_MS);\n});\n",
				"ours": ""
			}
		]
	},
	{
		"file": "test/control-egress.test.js"
	},
	{
		"file": "test/connection-slot-declaration.test.js"
	},
	{
		"file": "test/vite-upgrade-headers.test.js"
	},
	{
		"file": "test/resume-grant-gate-real.test.js"
	}
]
